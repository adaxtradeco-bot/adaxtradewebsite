/**
 * Admin Pages Management - With Multilingual Support
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 * Updated: 2024-01-20 - Added multilingual grouping
 */

'use client';

import { useState } from 'react';
import Toast from '@/components/ui/Toast';
import { PageListGrouped } from '@/components/admin/PageListGrouped';
import { PageListFlat } from '@/components/admin/PageListFlat';
import { ViewModeToggle } from '@/components/admin/ViewModeToggle';
import { CreatePageModal } from '@/components/admin/CreatePageModal';
import { AddTranslationModal } from '@/components/admin/AddTranslationModal';
import { useRouter } from 'next/navigation';

export default function AdminPages() {
  const router = useRouter();
  const [viewMode, setViewMode] = useState<'grouped' | 'flat'>('grouped');
  const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' } | null>(null);
  const [showCreateModal, setShowCreateModal] = useState(false);
  const [showAddTranslationModal, setShowAddTranslationModal] = useState(false);
  const [selectedPageId, setSelectedPageId] = useState<string>('');
  const [refreshKey, setRefreshKey] = useState(0);

  const handleCreateSuccess = (pageId: string) => {
    setToast({ message: 'Page created successfully!', type: 'success' });
    setRefreshKey(prev => prev + 1);
    // Navigate to page builder
    setTimeout(() => {
      router.push(`/admin/pages/builder/${pageId}`);
    }, 1000);
  };

  const handleAddTranslation = (pageId: string) => {
    setSelectedPageId(pageId);
    setShowAddTranslationModal(true);
  };

  const handleTranslationSuccess = () => {
    setToast({ message: 'Translation created successfully!', type: 'success' });
    setRefreshKey(prev => prev + 1);
    // Force immediate refresh
    setTimeout(() => {
      window.location.reload();
    }, 1000);
  };

  return (
    <div>
      <div className="flex justify-between items-center mb-8">
        <div>
          <h1 className="text-3xl font-bold text-gray-900 dark:text-white">
            Pages
          </h1>
          <p className="mt-2 text-gray-600 dark:text-gray-400">
            Manage your website pages and translations
          </p>
        </div>
        
        <div className="flex items-center gap-3">
          <ViewModeToggle value={viewMode} onChange={setViewMode} />
          <button
            onClick={() => setShowCreateModal(true)}
            className="bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg font-medium transition-colors"
          >
            + Create New Page
          </button>
        </div>
      </div>

      {/* Pages List */}
      <div key={refreshKey}>
        {viewMode === 'grouped' ? (
          <PageListGrouped onAddTranslation={handleAddTranslation} key={`grouped-${refreshKey}`} />
        ) : (
          <PageListFlat onAddTranslation={handleAddTranslation} key={`flat-${refreshKey}`} />
        )}
      </div>

      {/* Modals */}
      <CreatePageModal
        isOpen={showCreateModal}
        onClose={() => setShowCreateModal(false)}
        onSuccess={handleCreateSuccess}
      />

      <AddTranslationModal
        isOpen={showAddTranslationModal}
        onClose={() => setShowAddTranslationModal(false)}
        sourcePageId={selectedPageId}
        onSuccess={handleTranslationSuccess}
      />

      {toast && (
        <Toast
          message={toast.message}
          type={toast.type}
          onClose={() => setToast(null)}
        />
      )}
    </div>
  );
}