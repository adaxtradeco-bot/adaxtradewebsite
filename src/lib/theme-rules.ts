/**
 * Universal Theme Rules - Consistent theming across all components
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

export const themeRules = {
  // Background colors
  backgrounds: {
    primary: 'bg-white dark:bg-slate-900',
    secondary: 'bg-slate-50 dark:bg-slate-800',
    card: 'bg-white dark:bg-slate-800',
    cardHover: 'bg-slate-50 dark:bg-slate-700',
    overlay: 'bg-white/80 dark:bg-slate-800/80',
    glass: 'bg-white/5 dark:bg-white/5 bg-slate-900/5',
  },
  
  // Text colors
  text: {
    primary: 'text-slate-900 dark:text-white',
    secondary: 'text-slate-600 dark:text-slate-300',
    muted: 'text-slate-500 dark:text-slate-400',
    inverse: 'text-white dark:text-slate-900',
  },
  
  // Border colors
  borders: {
    primary: 'border-slate-200 dark:border-slate-700',
    secondary: 'border-slate-300 dark:border-slate-600',
    glass: 'border-white/10 dark:border-white/10 border-slate-900/20',
  },
  
  // Interactive states
  interactive: {
    hover: 'hover:bg-slate-50 dark:hover:bg-slate-800',
    focus: 'focus:ring-2 focus:ring-primary-500 dark:focus:ring-primary-400',
    active: 'active:bg-slate-100 dark:active:bg-slate-700',
  },
  
  // Gradients
  gradients: {
    hero: 'bg-gradient-to-br from-violet-50 via-slate-50 to-cyan-50 dark:from-violet-950 dark:via-slate-900 dark:to-cyan-950',
    overlay: 'bg-gradient-to-r from-violet-200/30 via-transparent to-cyan-200/30 dark:from-violet-600/20 dark:via-transparent dark:to-cyan-500/20',
    section: 'bg-gradient-to-b from-violet-100/20 to-transparent dark:from-violet-600/5 dark:to-transparent',
  }
} as const;

// Helper function to combine theme classes
export function getThemeClasses(type: keyof typeof themeRules, variant: string) {
  return themeRules[type][variant as keyof typeof themeRules[typeof type]] || '';
}