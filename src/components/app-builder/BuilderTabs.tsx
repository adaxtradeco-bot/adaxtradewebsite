/**
 * App Builder Tabs Component
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

'use client';

import { useState } from 'react';
import { useTranslation } from '@/hooks/useTranslation';

type TabType = 'ui' | 'flows' | 'themes' | 'code';

const tabContent = {
  ui: {
    title: 'Visual Drag-and-Drop Builder',
    description: 'Place forms, tables, charts, boards, and buttons; wire actions; set validations. Build pages without code.',
    features: [
      'Reusable components',
      'Conditional UI',
      'Inline validation',
      'Form states'
    ]
  },
  flows: {
    title: 'Flows & Business Rules',
    description: 'Configure steps, branches, and approvals. Use time-based SLAs and escalations. Trigger actions on events.',
    features: [
      'Parallel/Sequential approvals',
      'Timers & escalations',
      'Webhooks & callbacks',
      'Rule sets & conditions'
    ]
  },
  themes: {
    title: 'Design Systems & Themes',
    description: 'Apply brand colors, typography, and spacing system-wide. Keep UI consistent across workspaces.',
    features: [
      'Global variables',
      'Workspace themes',
      'Component tokens',
      'Dark/Light support'
    ]
  },
  code: {
    title: 'Extend with Code',
    description: 'Embed custom JS, call APIs, or write small functions where needed. No-code speed + code precision.',
    features: [
      'JS hooks',
      'REST/GraphQL',
      'SDK/Plugins',
      'Unit tests'
    ]
  }
};

const getTabFeatures = (tab: TabType) => {
  const features = {
    ui: ['reusableComponents', 'conditionalUI', 'inlineValidation', 'formStates'],
    flows: ['parallelApprovals', 'timersEscalations', 'webhooksCallbacks', 'ruleSetsConditions'],
    themes: ['globalVariables', 'workspaceThemes', 'componentTokens', 'darkLightSupport'],
    code: ['jsHooks', 'restGraphQL', 'sdkPlugins', 'unitTests']
  };
  return features[tab];
};

export function BuilderTabs() {
  const [activeTab, setActiveTab] = useState<TabType>('ui');
  const { t } = useTranslation();

  return (
    <section className="bg-slate-50 dark:bg-slate-800 text-slate-900 dark:text-white py-16">
      <div className="max-w-7xl mx-auto px-6">
        <div className="grid lg:grid-cols-[260px_1fr] gap-10">
          <aside>
            <div className="rounded-2xl bg-white dark:bg-slate-700 border border-slate-200 dark:border-slate-600 p-4 sticky top-6">
              {Object.keys(tabContent).map((tab) => (
                <button
                  key={tab}
                  onClick={() => setActiveTab(tab as TabType)}
                  className={`w-full text-left px-3 py-2 rounded-lg mb-2 transition-colors capitalize ${
                    activeTab === tab 
                      ? 'bg-slate-100 dark:bg-slate-600 text-slate-900 dark:text-white' 
                      : 'text-slate-600 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-600 hover:text-slate-900 dark:hover:text-white'
                  }`}
                >
                  {t(`components.tabs.${tab}.label`)}
                </button>
              ))}
            </div>
          </aside>
          
          <div className="rounded-2xl bg-white dark:bg-slate-700 border border-slate-200 dark:border-slate-600 p-8">
            <h2 className="text-3xl font-bold mb-3">
              {t(`components.tabs.${activeTab}.title`)}
            </h2>
            <p className="text-slate-600 dark:text-slate-300 mb-6 leading-relaxed">
              {t(`components.tabs.${activeTab}.description`)}
            </p>
            
            <div className="grid sm:grid-cols-2 gap-4 mb-6">
              {getTabFeatures(activeTab).map((feature, index) => (
                <div 
                  key={index}
                  className="p-3 rounded-lg bg-slate-50 dark:bg-slate-600 border border-slate-200 dark:border-slate-500 text-sm"
                >
                  {t(`components.appBuilder.tabFeatures.${feature}`)}
                </div>
              ))}
            </div>

            {activeTab === 'ui' && (
              <div className="aspect-[5/2] bg-slate-100 dark:bg-slate-600 rounded-2xl border border-slate-200 dark:border-slate-500 flex items-center justify-center">
                <div className="text-center text-slate-600 dark:text-slate-300">
                  <div className="text-4xl mb-2">🎨</div>
                  <p className="text-sm">{t('components.appBuilder.tabFeatures.canvasPreview')}</p>
                </div>
              </div>
            )}
          </div>
        </div>
      </div>
    </section>
  );
}