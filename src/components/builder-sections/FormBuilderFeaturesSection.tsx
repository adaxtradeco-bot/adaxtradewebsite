/**
 * Form Builder Features Section - Matching HTML Design
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 */

'use client';

import React from 'react';

interface FormBuilderFeaturesProps {
  data: {
    title: string;
    subtitle: string;
    badge: string;
    items: Array<{
      title: string;
      description: string;
      icon: string;
      features: string[];
    }>;
  };
  style?: any;
}

export default function FormBuilderFeaturesSection({ data }: FormBuilderFeaturesProps) {
  return (
    <section className="features-section">
      <style jsx>{`
        .features-section {
          padding: 5rem 2rem;
          max-width: 1400px;
          margin: 0 auto;
        }

        .section-header {
          text-align: center;
          margin-bottom: 4rem;
        }

        .section-badge {
          display: inline-block;
          padding: 0.5rem 1.2rem;
          background: rgba(139, 92, 246, 0.1);
          border-radius: 50px;
          color: #8B5CF6;
          font-weight: 700;
          font-size: 0.85rem;
          text-transform: uppercase;
          letter-spacing: 1px;
          margin-bottom: 1rem;
        }

        .section-header h2 {
          font-size: 2.8rem;
          font-weight: 900;
          color: #0f172a;
          margin-bottom: 1rem;
          letter-spacing: -2px;
        }

        .dark .section-header h2 {
          color: #ffffff;
        }

        .section-header p {
          font-size: 1.15rem;
          color: #475569;
          max-width: 700px;
          margin: 0 auto;
        }

        .dark .section-header p {
          color: #94a3b8;
        }

        .features-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
          gap: 2.5rem;
        }

        .feature-card {
          background: white;
          padding: 2.5rem;
          border-radius: 20px;
          box-shadow: 0 10px 40px rgba(0,0,0,0.08);
          transition: all 0.4s;
          border: 1px solid #e2e8f0;
        }

        .dark .feature-card {
          background: rgba(255, 255, 255, 0.05);
          box-shadow: 0 10px 40px rgba(0,0,0,0.3);
          border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .feature-card:hover {
          transform: translateY(-10px);
          box-shadow: 0 20px 60px rgba(0,0,0,0.12);
          border-color: #8B5CF6;
        }

        .dark .feature-card:hover {
          box-shadow: 0 20px 60px rgba(0,0,0,0.4);
        }

        .feature-icon {
          width: 70px;
          height: 70px;
          background: linear-gradient(135deg, #8B5CF6 0%, #7C3AED 100%);
          border-radius: 16px;
          display: flex;
          align-items: center;
          justify-content: center;
          font-size: 2.5rem;
          margin-bottom: 2rem;
        }

        .feature-card h3 {
          font-size: 1.6rem;
          font-weight: 800;
          color: #0f172a;
          margin-bottom: 1rem;
        }

        .dark .feature-card h3 {
          color: #ffffff;
        }

        .feature-card p {
          color: #475569;
          line-height: 1.8;
          margin-bottom: 1.5rem;
        }

        .dark .feature-card p {
          color: #94a3b8;
        }

        .feature-list {
          list-style: none;
          margin: 0;
          padding: 0;
        }

        .feature-list li {
          padding: 0.6rem 0;
          display: flex;
          align-items: center;
          gap: 0.8rem;
          font-size: 0.95rem;
          color: #334155;
        }

        .dark .feature-list li {
          color: #e2e8f0;
        }

        .feature-list li::before {
          content: '→';
          color: #8B5CF6;
          font-weight: 900;
        }
      `}</style>
      
      <div className="section-header">
        <div className="section-badge">{data.badge}</div>
        <h2>{data.title}</h2>
        <p>{data.subtitle}</p>
      </div>
      
      <div className="features-grid">
        {data.items.map((item, index) => (
          <div key={index} className="feature-card">
            <div className="feature-icon">{item.icon}</div>
            <h3>{item.title}</h3>
            <p>{item.description}</p>
            <ul className="feature-list">
              {item.features.map((feature, featureIndex) => (
                <li key={featureIndex}>{feature}</li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </section>
  );
}