/**
 * ReportsAIInsightsSection
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 */
'use client';
import React from 'react';

interface AICard { num: string; title: string; description: string; }

interface ReportsAIInsightsSectionData {
  eyebrow?: string;
  title?: string;
  titleGradient?: string;
  subtitle?: string;
  cards?: AICard[];
  accentColor?: string;
  accentColor2?: string;
}

interface Props {
  data: ReportsAIInsightsSectionData;
  style?: { backgroundColor?: string; textColor?: string; padding?: string };
  isBuilder?: boolean;
}

const DEFAULT_CARDS: AICard[] = [
  { num: '01', title: 'Detect bottlenecks', description: 'Automatically identify delays and anomalies in your workflows before they escalate.' },
  { num: '02', title: 'Alert on thresholds', description: 'Set KPI limits. Get notified the moment something crosses a critical boundary.' },
  { num: '03', title: 'Predict trends', description: 'Surface patterns before they become problems with AI-driven forecasting.' },
  { num: '04', title: 'Trigger actions', description: 'Escalate tasks, run scripts, and send alerts — all from inside the dashboard.' },
];

export default function ReportsAIInsightsSection({ data, isBuilder = false }: Props) {
  const {
    eyebrow = 'AI-powered insights',
    title = "Your dashboards don't just show data.",
    titleGradient = 'They act on it.',
    subtitle = "Connect to Ivaflow's AI engine and turn passive reports into proactive systems.",
    cards = DEFAULT_CARDS,
    accentColor = '#6366f1',
    accentColor2 = '#818cf8',
  } = data;

  const grad = `linear-gradient(135deg, ${accentColor}, #8b5cf6, #06b6d4)`;

  return (
    <section className="py-20 md:py-24 border-t border-b border-slate-200 dark:border-white/[0.06] bg-indigo-950 dark:bg-[#020617]">
      <div className="max-w-[1000px] mx-auto px-5 md:px-8">
        <p className="text-[11px] font-semibold tracking-[0.1em] uppercase mb-3" style={{ color: accentColor2 }}>{eyebrow}</p>
        <h2 className="font-extrabold leading-[1.12] tracking-tight mb-3 text-white" style={{ fontSize: 'clamp(1.7rem, 3.5vw, 2.5rem)' }}>
          {title}<br />
          <span style={{ background: grad, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>{titleGradient}</span>
        </h2>
        <p className="text-[15px] leading-[1.7] mb-8 max-w-[520px] text-indigo-200/60">{subtitle}</p>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3">
          {cards.map((c, i) => (
            <div
              key={i}
              className="relative rounded-2xl p-6 overflow-hidden bg-white/[0.05] border border-white/[0.09]"
            >
              <div className="absolute top-0 left-0 right-0 h-0.5" style={{ background: grad }} />
              <div className="text-[28px] font-black mb-3" style={{ color: `${accentColor}55` }}>{c.num}</div>
              <h3 className="text-[14px] font-semibold text-white mb-1.5">{c.title}</h3>
              <p className="text-[13px] leading-[1.6] text-indigo-200/50">{c.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
