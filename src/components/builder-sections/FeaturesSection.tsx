/**
 * Builder Features Section Component
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

import React from 'react';
import { FeaturesSectionConfig } from '@/lib/page-builder/section-schemas';

interface FeaturesSectionProps {
  section: FeaturesSectionConfig;
  isBuilder?: boolean;
}

export function FeaturesSection({ section, isBuilder = false }: FeaturesSectionProps) {
  const { data, style } = section;

  return (
    <section 
      className={`${style?.backgroundColor || 'bg-white'} 
                  ${style?.textColor || 'text-slate-900'} 
                  ${style?.padding || 'py-16'} 
                  dark:bg-slate-900 dark:text-white`}
    >
      <div className="max-w-7xl mx-auto px-6">
        {/* Header */}
        <div className={`${style?.alignment === 'left' ? 'text-left' : 
                         style?.alignment === 'right' ? 'text-right' : 'text-center'} 
                        mb-12`}>
          {data.subtitle && (
            <p className="text-sm font-medium text-violet-600 dark:text-violet-400 mb-2 uppercase tracking-wide">
              {data.subtitle}
            </p>
          )}
          
          <h2 className="text-3xl md:text-4xl font-bold mb-4">
            {data.title}
          </h2>
          
          {data.description && (
            <p className="text-lg text-slate-600 dark:text-slate-300 max-w-3xl mx-auto">
              {data.description}
            </p>
          )}
        </div>

        {/* Features Grid */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
          {(data.items || []).map((feature, index) => (
            <div
              key={index}
              className="group p-6 rounded-2xl bg-slate-50 dark:bg-slate-800 hover:bg-white dark:hover:bg-slate-700 transition-all duration-300 hover:shadow-lg hover:-translate-y-1"
            >
              {/* Icon */}
              {feature.icon && (
                <div className="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">
                  {feature.icon}
                </div>
              )}

              {/* Title */}
              <h3 className="text-xl font-semibold mb-3 text-slate-900 dark:text-white">
                {typeof feature === 'string' ? feature : feature.title}
              </h3>

              {/* Description */}
              {typeof feature === 'object' && feature.description && (
                <p className="text-slate-600 dark:text-slate-300 leading-relaxed">
                  {feature.description}
                </p>
              )}
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}