/**
 * ReportsDataSourcesSection
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 */
'use client';
import React from 'react';

interface DataSource { icon: string; name: string; type: string; }

interface ReportsDataSourcesSectionData {
  eyebrow?: string;
  title?: string;
  titleGradient?: string;
  description?: string;
  sources?: DataSource[];
  accentColor?: string;
  accentColor2?: string;
}

interface Props {
  data: ReportsDataSourcesSectionData;
  style?: { backgroundColor?: string; textColor?: string; padding?: string };
  isBuilder?: boolean;
}

const DEFAULT_SOURCES: DataSource[] = [
  { icon: '⚙', name: 'Ivaflow Forms', type: 'Internal' },
  { icon: '⚙', name: 'Workflows & SLA', type: 'Internal' },
  { icon: '🗄', name: 'SQL Server', type: 'Database' },
  { icon: '🗄', name: 'PostgreSQL', type: 'Database' },
  { icon: '🗄', name: 'MySQL', type: 'Database' },
  { icon: '🔌', name: 'REST APIs', type: 'Web service' },
  { icon: '📂', name: 'Excel / CSV', type: 'File' },
  { icon: '📋', name: 'Stored Procs', type: 'Custom model' },
];

export default function ReportsDataSourcesSection({ data, isBuilder = false }: Props) {
  const {
    eyebrow = 'Data connectivity',
    title = 'Connect',
    titleGradient = 'everything',
    description = 'No SQL, no API keys in your dashboards. Every connection built through an intuitive visual interface.',
    sources = DEFAULT_SOURCES,
    accentColor = '#6366f1',
    accentColor2 = '#818cf8',
  } = data;

  const grad = `linear-gradient(135deg, ${accentColor}, #8b5cf6, #06b6d4)`;

  return (
    <section className="py-20 md:py-24 bg-white dark:bg-[#080810] border-t border-slate-200 dark:border-white/[0.06]">
      <div className="max-w-[1000px] mx-auto px-5 md:px-8">
        <p className="text-[11px] font-semibold tracking-[0.1em] uppercase mb-3" style={{ color: accentColor2 }}>{eyebrow}</p>
        <h2 className="font-extrabold leading-[1.12] tracking-tight mb-3 text-slate-900 dark:text-white" style={{ fontSize: 'clamp(1.7rem, 3.5vw, 2.5rem)' }}>
          {title}{' '}
          <span style={{ background: grad, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>{titleGradient}</span>
          {' '}— visually
        </h2>
        <p className="text-[15px] leading-[1.7] text-slate-500 dark:text-[#8080a8] mb-8 max-w-[520px]">{description}</p>

        <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2.5">
          {sources.map((s, i) => (
            <div
              key={i}
              className="rounded-xl p-4 transition-colors duration-200 cursor-default
                bg-white dark:bg-[#12121e]
                border border-slate-200 dark:border-white/[0.06]
                hover:border-indigo-300 dark:hover:border-indigo-500/40"
            >
              <div className="text-xl mb-2">{s.icon}</div>
              <div className="text-[13px] font-semibold text-slate-800 dark:text-white mb-0.5">{s.name}</div>
              <div className="text-[11px] text-slate-400 dark:text-[#404060]">{s.type}</div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
