/**
 * Form Builder BPMS Section - Use Case Carousel
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 */

'use client';

import React, { useRef } from 'react';

interface FormBuilderBPMSProps {
  data: {
    title: string;
    description: string;
    useCases: Array<{
      title: string;
      description: string;
    }>;
  };
  style?: any;
}

export default function FormBuilderBPMSSection({ data }: FormBuilderBPMSProps) {
  const scrollRef = useRef<HTMLDivElement>(null);

  const scroll = (direction: 'left' | 'right') => {
    if (scrollRef.current) {
      const scrollAmount = 336; // card width + gap
      scrollRef.current.scrollBy({
        left: direction === 'left' ? -scrollAmount : scrollAmount,
        behavior: 'smooth'
      });
    }
  };

  return (
    <section className="bg-gradient-to-b from-violet-50 dark:from-purple-500/8 to-transparent">
      <div className="mx-auto max-w-7xl px-6 py-16">
        <div className="flex items-end justify-between mb-6">
          <div>
            <h2 className="text-3xl md:text-4xl font-bold text-slate-900 dark:text-white">{data.title}</h2>
            <p className="mt-3 text-slate-600 dark:text-slate-300 max-w-3xl">{data.description}</p>
          </div>
          <div className="flex gap-2">
            <button 
              onClick={() => scroll('left')}
              className="px-3 py-2 rounded-lg bg-slate-100 dark:bg-white/10 hover:bg-slate-200 dark:hover:bg-white/15 text-slate-700 dark:text-white transition-colors border border-slate-200 dark:border-white/10"
            >
              ‹
            </button>
            <button 
              onClick={() => scroll('right')}
              className="px-3 py-2 rounded-lg bg-slate-100 dark:bg-white/10 hover:bg-slate-200 dark:hover:bg-white/15 text-slate-700 dark:text-white transition-colors border border-slate-200 dark:border-white/10"
            >
              ›
            </button>
          </div>
        </div>
        
        <div className="rounded-2xl border border-slate-200 dark:border-white/10 bg-white dark:bg-white/5 overflow-hidden shadow-sm">
          <div 
            ref={scrollRef}
            className="flex gap-4 p-6 overflow-x-auto scrollbar-thin scrollbar-thumb-slate-300 dark:scrollbar-thumb-white/15 scrollbar-track-transparent"
            style={{ scrollbarWidth: 'thin' }}
          >
            {data.useCases.map((useCase, index) => {
              const gradients = [
                'bg-gradient-to-br from-indigo-50 to-indigo-100 border-indigo-200 text-indigo-900',
                'bg-gradient-to-br from-pink-50 to-pink-100 border-pink-200 text-pink-900',
                'bg-gradient-to-br from-lime-50 to-lime-100 border-lime-200 text-lime-900',
                'bg-gradient-to-br from-sky-50 to-sky-100 border-sky-200 text-sky-900',
                'bg-gradient-to-br from-fuchsia-50 to-fuchsia-100 border-fuchsia-200 text-fuchsia-900'
              ];
              const textColors = [
                'text-indigo-700',
                'text-pink-700',
                'text-lime-700',
                'text-sky-700',
                'text-fuchsia-700'
              ];
              return (
                <div 
                  key={index} 
                  className={`min-w-80 rounded-xl border p-6 hover:transform hover:-translate-y-1 transition-all duration-300 hover:shadow-lg flex-shrink-0 shadow-sm lift dark:bg-white/5 dark:border-white/10 ${gradients[index % gradients.length]}`}
                >
                  <div className="text-lg font-semibold mb-2 dark:text-white">{useCase.title}</div>
                  <p className={`text-sm mt-1 dark:text-slate-300 ${textColors[index % textColors.length]}`}>{useCase.description}</p>
                </div>
              );
            })}
          </div>
        </div>
      </div>
    </section>
  );
}