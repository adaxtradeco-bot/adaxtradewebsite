/**
 * Form Builder Mobile & Voice Section - Enhanced with Icons & Infographics
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 * Updated: 2025-01-XX - Added infographic support, FA icons, gradient titles, theme contrast
 */

'use client';

import React from 'react';
import InfographicRenderer from './InfographicRenderer';

interface FormBuilderMobileVoiceProps {
  data: {
    title: string;
    description: string;
    features: Array<{
      title: string;
      description: string;
      icon?: string;
      infographic?: {
        type: string;
        data: any;
      };
    }>;
    illustrationAlt: string;
  };
  style?: any;
}

export default function FormBuilderMobileVoiceSection({ data }: FormBuilderMobileVoiceProps) {
  const themeConfigs = [
    {
      gradient: 'from-blue-500 via-cyan-500 to-blue-600',
      bg: 'bg-gradient-to-br from-blue-50/80 to-cyan-50/80 dark:from-blue-950/30 dark:to-cyan-950/30',
      border: 'border-blue-200/50 dark:border-blue-500/20',
      iconBg: 'bg-blue-100 dark:bg-blue-900/40',
      iconText: 'text-blue-600 dark:text-blue-400',
      text: 'text-blue-900 dark:text-blue-100'
    },
    {
      gradient: 'from-emerald-500 via-teal-500 to-emerald-600',
      bg: 'bg-gradient-to-br from-emerald-50/80 to-teal-50/80 dark:from-emerald-950/30 dark:to-teal-950/30',
      border: 'border-emerald-200/50 dark:border-emerald-500/20',
      iconBg: 'bg-emerald-100 dark:bg-emerald-900/40',
      iconText: 'text-emerald-600 dark:text-emerald-400',
      text: 'text-emerald-900 dark:text-emerald-100'
    },
    {
      gradient: 'from-violet-500 via-purple-500 to-violet-600',
      bg: 'bg-gradient-to-br from-violet-50/80 to-purple-50/80 dark:from-violet-950/30 dark:to-purple-950/30',
      border: 'border-violet-200/50 dark:border-violet-500/20',
      iconBg: 'bg-violet-100 dark:bg-violet-900/40',
      iconText: 'text-violet-600 dark:text-violet-400',
      text: 'text-violet-900 dark:text-violet-100'
    }
  ];

  return (
    <section className="bg-gradient-to-b from-cyan-50/50 to-transparent dark:from-cyan-950/20 dark:to-transparent">
      <div className="mx-auto max-w-7xl px-6 py-20">
        <div className="text-center max-w-3xl mx-auto mb-12">
          <h2 className="text-4xl md:text-5xl font-bold bg-gradient-to-r from-cyan-600 via-blue-600 to-violet-600 dark:from-cyan-400 dark:via-blue-400 dark:to-violet-400 bg-clip-text text-transparent">
            {data.title}
          </h2>
          <p className="mt-4 text-lg text-slate-600 dark:text-slate-300">{data.description}</p>
        </div>
        
        <div className="grid md:grid-cols-3 gap-6 mb-12">
          {data.features.map((feature, index) => {
            const theme = themeConfigs[index % themeConfigs.length];
            return (
              <div 
                key={index} 
                className={`rounded-2xl border-2 p-6 hover:transform hover:-translate-y-2 transition-all duration-300 hover:shadow-2xl shadow-lg backdrop-blur-sm ${theme.bg} ${theme.border}`}
              >
                {/* Icon Header */}
                {feature.icon && (
                  <div className={`w-14 h-14 rounded-xl ${theme.iconBg} flex items-center justify-center mb-4 shadow-md`}>
                    <i className={`${feature.icon} text-2xl ${theme.iconText}`}></i>
                  </div>
                )}

                {/* Title with Gradient */}
                <h3 className={`text-xl font-bold mb-3 bg-gradient-to-r ${theme.gradient} bg-clip-text text-transparent`}>
                  {feature.title}
                </h3>

                {/* Description */}
                <p className={`text-sm leading-relaxed ${theme.text}`}>
                  {feature.description}
                </p>

                {/* Infographic */}
                {feature.infographic && feature.infographic.type && (
                  <div className="mt-4 pt-4 border-t border-slate-200/50 dark:border-slate-700/50">
                    <InfographicRenderer 
                      infographic={feature.infographic}
                    />
                  </div>
                )}
              </div>
            );
          })}
        </div>
        
        <figure className="rounded-2xl overflow-hidden border-2 border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 shadow-2xl">
          <img 
            src="https://placehold.co/1200x360/f1f5f9/475569?text=Phones+%2F+Voice+Flow+Illustration" 
            alt={data.illustrationAlt}
            className="w-full h-auto"
          />
          <figcaption className="text-xs text-slate-500 dark:text-slate-400 p-4 border-t border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-900/50">
            Mock: mobile screens + IVR flow showing voice form capture.
          </figcaption>
        </figure>
      </div>
    </section>
  );
}