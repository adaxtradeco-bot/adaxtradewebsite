/**
 * Form Builder Mobile & Voice Section
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 */

'use client';

import React from 'react';

interface FormBuilderMobileVoiceProps {
  data: {
    title: string;
    description: string;
    features: Array<{
      title: string;
      description: string;
    }>;
    illustrationAlt: string;
  };
  style?: any;
}

export default function FormBuilderMobileVoiceSection({ data }: FormBuilderMobileVoiceProps) {
  return (
    <section className="bg-gradient-to-b from-cyan-500/8 dark:from-cyan-500/8 from-cyan-50 to-transparent">
      <div className="mx-auto max-w-7xl px-6 py-16">
        <div className="text-center max-w-3xl mx-auto mb-8">
          <h2 className="text-3xl md:text-4xl font-bold text-slate-900 dark:text-white">{data.title}</h2>
          <p className="mt-3 text-slate-600 dark:text-slate-300">{data.description}</p>
        </div>
        
        <div className="grid md:grid-cols-3 gap-5 mb-8">
          {data.features.map((feature, index) => (
            <div 
              key={index} 
              className="rounded-2xl bg-white dark:bg-white/5 border border-slate-200 dark:border-white/10 p-5 hover:transform hover:-translate-y-1 transition-all duration-300 hover:shadow-lg shadow-sm lift"
            >
              <h3 className="font-semibold text-slate-900 dark:text-white mb-2">{feature.title}</h3>
              <p className="text-sm text-slate-600 dark:text-slate-300 mt-2">{feature.description}</p>
            </div>
          ))}
        </div>
        
        <figure className="rounded-2xl overflow-hidden border border-white/20 dark:border-white/20 border-slate-200 bg-white/3 dark:bg-white/3 bg-white shadow-lg">
          <img 
            src="https://placehold.co/1200x360/f1f5f9/475569?text=Phones+%2F+Voice+Flow+Illustration" 
            alt={data.illustrationAlt}
            className="w-full h-auto"
          />
          <figcaption className="text-xs text-slate-400 dark:text-slate-400 text-slate-500 p-3 border-t border-white/15 dark:border-white/15 border-slate-200">
            Mock: mobile screens + IVR flow showing voice form capture.
          </figcaption>
        </figure>
      </div>
    </section>
  );
}