/**
 * WhyIvaFlowNewVersion - Advanced why section with unified OS showcase
 * Author: generated by Amazon Q
 * Created: 2024-12-19
 */

'use client';

import React from 'react';
import { WhyIvaFlowNewVersionData } from '@/lib/page-builder/why-ivaflow-new-version';

interface WhyIvaFlowNewVersionProps {
  data: WhyIvaFlowNewVersionData;
}

export default function WhyIvaFlowNewVersion({ data }: WhyIvaFlowNewVersionProps) {
  const {
    header,
    unifiedOS,
    cards,
    styling
  } = data;

  return (
    <section 
      className={`relative overflow-hidden py-12 md:py-20 px-4 md:px-6 ${styling.backgroundColor}`}
    >
      {/* BACKDROP */}
      <div className="absolute inset-0">
        <div className={`absolute inset-0 ${styling.gradientBackground}`}></div>

        {/* soft glows */}
        <div className={`pointer-events-none absolute -top-24 -right-24 h-72 w-72 rounded-full ${styling.topGlow} blur-3xl`}></div>
        <div className={`pointer-events-none absolute -bottom-28 -left-28 h-80 w-80 rounded-full ${styling.bottomGlow} blur-3xl`}></div>

        {/* subtle grid */}
        <div 
          className={`pointer-events-none absolute inset-0 ${styling.gridOpacity}`}
          style={{
            backgroundImage: `linear-gradient(to right, rgba(15,23,42,0.02) 1px, transparent 1px), linear-gradient(to bottom, rgba(15,23,42,0.02) 1px, transparent 1px)`,
            backgroundSize: '24px 24px'
          }}
        ></div>

        {/* noise layer */}
        <div 
          className={`pointer-events-none absolute inset-0 ${styling.noiseOpacity}`}
          style={{
            backgroundImage: "url('data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22200%22 height=%22200%22%3E%3Cfilter id=%22n%22%3E%3CfeTurbulence type=%22fractalNoise%22 baseFrequency=%220.9%22 numOctaves=%222%22 stitchTiles=%22stitch%22/%3E%3C/filter%3E%3Crect width=%22200%22 height=%22200%22 filter=%22url(%23n)%22 opacity=%220.3%22/%3E%3C/svg%3E')"
          }}
        ></div>
      </div>

      <div className="relative z-10 max-w-7xl mx-auto">
        {/* HEADER */}
        <div className="max-w-2xl mx-auto text-center mb-8 md:mb-12">
          <span className={`inline-flex items-center gap-2 text-[11px] uppercase tracking-[0.25em] ${header.badgeTextColor}`}>
            <span className={`h-1.5 w-1.5 rounded-full ${header.badgeIconColor}`}></span>
            {header.badge}
          </span>

          <h2 className={`mt-3 text-2xl md:text-3xl lg:text-4xl font-bold ${header.titleColor}`}>
            {header.title}
          </h2>

          <p className={`mt-3 text-base md:text-lg ${header.descriptionColor}`}>
            {header.description}
          </p>
        </div>

        {/* MINI INFOGRAPHIC (ONE OS) */}
        <div className="max-w-4xl mx-auto mb-8 md:mb-12">
          <div className={`relative overflow-hidden rounded-2xl md:rounded-3xl border ${unifiedOS.borderColor} ${unifiedOS.backgroundColor} backdrop-blur-sm ${unifiedOS.shadowClass}`}>
            {/* edge highlight */}
            <div className={`pointer-events-none absolute inset-0 bg-gradient-to-br from-white/20 via-white/5 to-transparent dark:from-white/8 dark:via-white/2 dark:to-transparent`}></div>

            <div className="relative p-4 md:p-6">
              <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
                <div>
                  <div className={`inline-flex items-center gap-2 rounded-full border ${unifiedOS.badge.borderColor} ${unifiedOS.badge.backgroundColor} px-3 py-1 text-xs ${unifiedOS.badge.textColor}`}>
                    <span className={`h-1.5 w-1.5 rounded-full ${unifiedOS.badge.iconColor}`}></span>
                    {unifiedOS.badge.text}
                  </div>
                  <p className={`mt-2 text-sm md:text-base ${unifiedOS.descriptionColor}`}>
                    {unifiedOS.description}
                  </p>
                </div>

                {/* system map */}
                <div className="grid grid-cols-3 gap-2 md:gap-3">
                  {unifiedOS.systemItems.map((item, index) => (
                    <div key={index} className={`group rounded-xl border ${unifiedOS.itemBorderColor} ${unifiedOS.itemBackgroundColor} px-2 py-2 text-xs ${unifiedOS.itemTextColor}`}>
                      <div className="flex items-center gap-1">
                        <span className={`h-6 w-6 rounded-lg ${item.iconBg} grid place-items-center ${item.iconColor} text-sm flex-shrink-0`}>
                          <i className={item.icon}></i>
                        </span>
                        <span className="truncate text-xs">{item.label}</span>
                      </div>
                    </div>
                  ))}

                  <div className="col-span-3">
                    <div className={`relative mt-1 rounded-xl border ${unifiedOS.itemBorderColor} ${unifiedOS.itemBackgroundColor} px-3 py-2 text-xs ${unifiedOS.itemTextColor} overflow-hidden`}>
                      <div className={`absolute inset-0 ${unifiedOS.integrationGradient}`}></div>
                      <div className="relative flex items-center justify-between gap-2">
                        <span className="inline-flex items-center gap-2">
                          <span className={`h-7 w-7 rounded-lg ${unifiedOS.integrationIconBg} grid place-items-center ${unifiedOS.integrationIconColor}`}>
                            <i className={unifiedOS.integrationIcon}></i>
                          </span>
                          {unifiedOS.integrationLabel}
                        </span>
                        <span className={`text-[11px] ${unifiedOS.integrationSubtextColor}`}>
                          {unifiedOS.integrationSubtext}
                        </span>
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              {/* connection line */}
              <div className={`mt-4 h-px w-full ${unifiedOS.connectionLineGradient}`}></div>

              {/* value chips */}
              <div className="mt-4 flex flex-wrap items-center justify-center gap-2">
                {unifiedOS.valueChips.map((chip, index) => (
                  <span key={index} className={`inline-flex items-center gap-2 rounded-full border ${unifiedOS.chipBorderColor} ${unifiedOS.chipBackgroundColor} px-3 py-1 text-xs ${unifiedOS.chipTextColor}`}>
                    <span className={`h-1.5 w-1.5 rounded-full ${chip.iconColor}`}></span>
                    {chip.text}
                  </span>
                ))}
              </div>
            </div>
          </div>
        </div>

        {/* CARDS */}
        <div className="grid md:grid-cols-3 gap-4 md:gap-6">
          {cards.map((card, index) => (
            <article key={index} className={`group relative overflow-hidden rounded-2xl md:rounded-3xl border ${card.borderColor} ${card.backgroundColor} backdrop-blur-sm p-5 md:p-6 ${card.shadowClass} transition-all duration-500 hover:-translate-y-1 ${card.hoverShadowClass}`}>
              {/* glow accent */}
              <div className={`pointer-events-none absolute -top-16 -right-16 h-44 w-44 rounded-full ${card.glowColor} blur-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-500`}></div>
              {/* edge shine */}
              <div className={`pointer-events-none absolute inset-0 bg-gradient-to-br from-white/30 via-white/5 to-transparent dark:from-white/5 dark:via-white/2 dark:to-transparent`}></div>

              <div className="relative">
                <div className="flex items-start gap-3">
                  <span className={`h-10 w-10 rounded-2xl ${card.iconBackground} grid place-items-center ${card.iconColor} flex-shrink-0`}>
                    <i className={card.icon}></i>
                  </span>
                  <div className="flex-1 min-w-0">
                    <h3 className={`text-lg md:text-xl font-bold ${card.titleColor} leading-tight`}>
                      {card.title}
                    </h3>
                    <p className={`text-xs ${card.subtitleColor} mt-1`}>
                      {card.subtitle}
                    </p>
                  </div>
                  {card.badge && (
                    <span className={`flex-shrink-0 inline-flex items-center gap-2 rounded-full border ${card.badge.borderColor} ${card.badge.backgroundColor} px-2 py-1 text-xs ${card.badge.textColor}`}>
                      {card.badge.text}
                    </span>
                  )}
                </div>

                <p className={`mt-4 text-sm md:text-base ${card.descriptionColor}`}>
                  {card.description}
                </p>

                {card.features && (
                  <ul className={`mt-4 space-y-2 text-sm ${card.featuresTextColor}`}>
                    {card.features.map((feature, featureIndex) => (
                      <li key={featureIndex} className="flex items-start gap-2">
                        <span className={`mt-1 h-2 w-2 rounded-full ${card.featureBulletColor}`}></span>
                        {feature}
                      </li>
                    ))}
                  </ul>
                )}

                {card.grid && (
                  <div className="mt-4 grid grid-cols-2 gap-2">
                    {card.grid.map((gridItem, gridIndex) => (
                      <div key={gridIndex} className={`rounded-xl border ${card.gridItemBorderColor} ${card.gridItemBackgroundColor} p-3`}>
                        <p className={`text-xs ${card.gridItemLabelColor}`}>{gridItem.label}</p>
                        <p className={`text-sm font-medium ${card.gridItemValueColor}`}>{gridItem.value}</p>
                      </div>
                    ))}
                  </div>
                )}

                <div className={`mt-5 h-px w-full ${card.separatorGradient}`}></div>

                <div className="mt-4 flex items-center justify-between">
                  <span className={`text-xs ${card.outcomeLabel.color}`}>
                    {card.outcomeLabel.text}
                  </span>
                  <span className={`text-xs font-medium ${card.outcomeValue.color}`}>
                    {card.outcomeValue.text}
                  </span>
                </div>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  );
}