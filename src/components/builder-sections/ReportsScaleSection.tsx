/**
 * ReportsScaleSection - By the Numbers
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 */
'use client';
import React, { useEffect, useRef, useState } from 'react';

interface ScaleStat { icon: string; value: string; numericTarget?: number; suffix?: string; label: string; description: string; color: string; iconBg: string; iconBorder: string; glowColor: string; wide?: boolean; }
interface WideBadge { text: string; colorClass: string; }

interface ReportsScaleSectionData {
  eyebrow?: string;
  title?: string;
  titleGradient?: string;
  stats?: ScaleStat[];
  wideTitle?: string;
  wideDescription?: string;
  wideBadges?: WideBadge[];
  accentColor?: string;
  accentColor2?: string;
}

interface Props {
  data: ReportsScaleSectionData;
  style?: { backgroundColor?: string; textColor?: string; padding?: string };
  isBuilder?: boolean;
}

const DEFAULT_STATS: ScaleStat[] = [
  { icon: '📊', value: '10+', numericTarget: 10, suffix: '+', label: 'Chart types', description: 'Bar, line, area, pie, scatter, heatmap, combo, gauge, funnel, and KPI cards.', color: '#818cf8', iconBg: 'rgba(99,102,241,0.12)', iconBorder: 'rgba(99,102,241,0.2)', glowColor: 'rgba(99,102,241,0.25)' },
  { icon: '🔌', value: '8+', numericTarget: 8, suffix: '+', label: 'Data connectors', description: 'Connect to internal Ivaflow data, SQL databases, REST APIs, Excel, and more.', color: '#06b6d4', iconBg: 'rgba(6,182,212,0.12)', iconBorder: 'rgba(6,182,212,0.2)', glowColor: 'rgba(6,182,212,0.2)' },
  { icon: '⚡', value: 'Real-time', label: 'Live data refresh', description: 'Dashboards update instantly when forms are submitted or workflows progress.', color: '#a78bfa', iconBg: 'rgba(139,92,246,0.12)', iconBorder: 'rgba(139,92,246,0.2)', glowColor: 'rgba(139,92,246,0.2)' },
  { icon: '✓', value: '0', numericTarget: 0, suffix: '', label: 'Lines of code required', description: 'Every dashboard, chart, and automation built entirely through a visual interface.', color: '#10b981', iconBg: 'rgba(16,185,129,0.12)', iconBorder: 'rgba(16,185,129,0.2)', glowColor: 'rgba(16,185,129,0.18)' },
];

const DEFAULT_BADGES: WideBadge[] = [
  { text: 'Forms & Apps', colorClass: 'indigo' },
  { text: 'Workflow engine', colorClass: 'cyan' },
  { text: 'SLA tracking', colorClass: 'violet' },
  { text: 'Script Manager', colorClass: 'green' },
  { text: 'AI insights', colorClass: 'indigo' },
  { text: 'Communication records', colorClass: 'cyan' },
  { text: 'Customer portals', colorClass: 'violet' },
  { text: 'Embedded views', colorClass: 'green' },
];

const BADGE_STYLES: Record<string, string> = {
  indigo: 'bg-indigo-100 dark:bg-indigo-500/10 border-indigo-200 dark:border-indigo-500/25 text-indigo-600 dark:text-indigo-300',
  cyan: 'bg-cyan-100 dark:bg-cyan-500/10 border-cyan-200 dark:border-cyan-500/25 text-cyan-600 dark:text-cyan-300',
  violet: 'bg-violet-100 dark:bg-violet-500/10 border-violet-200 dark:border-violet-500/25 text-violet-600 dark:text-violet-300',
  green: 'bg-emerald-100 dark:bg-emerald-500/10 border-emerald-200 dark:border-emerald-500/25 text-emerald-600 dark:text-emerald-300',
};

function AnimatedNumber({ target, suffix, active }: { target: number; suffix: string; active: boolean }) {
  const [val, setVal] = useState(0);
  useEffect(() => {
    if (!active) return;
    const dur = 1200, start = performance.now();
    const step = (now: number) => {
      const p = Math.min((now - start) / dur, 1);
      const e = 1 - Math.pow(1 - p, 3);
      setVal(Math.round(e * target));
      if (p < 1) requestAnimationFrame(step);
    };
    requestAnimationFrame(step);
  }, [active, target]);
  return <>{val}{suffix}</>;
}

export default function ReportsScaleSection({ data, isBuilder = false }: Props) {
  const {
    eyebrow = 'By the numbers',
    title = 'Built for',
    titleGradient = 'scale',
    stats = DEFAULT_STATS,
    wideTitle = 'Connects to your entire stack',
    wideDescription = 'Every module in Ivaflow feeds your dashboards automatically — no manual syncing, no data silos.',
    wideBadges = DEFAULT_BADGES,
    accentColor = '#6366f1',
    accentColor2 = '#818cf8',
  } = data;

  const grad = `linear-gradient(135deg, ${accentColor}, #8b5cf6, #06b6d4)`;
  const sectionRef = useRef<HTMLDivElement>(null);
  const [animated, setAnimated] = useState(isBuilder);

  useEffect(() => {
    if (isBuilder) { setAnimated(true); return; }
    const obs = new IntersectionObserver(entries => {
      if (entries[0].isIntersecting) { setAnimated(true); obs.disconnect(); }
    }, { threshold: 0.3 });
    if (sectionRef.current) obs.observe(sectionRef.current);
    return () => obs.disconnect();
  }, [isBuilder]);

  return (
    <section ref={sectionRef} className="py-20 md:py-28 bg-white dark:bg-[#020617] border-t border-slate-200 dark:border-white/[0.06]">
      <div className="max-w-[1000px] mx-auto px-5 md:px-8">
        <div className="text-center mb-12">
          <p className="text-[11px] font-semibold tracking-[0.1em] uppercase mb-3" style={{ color: accentColor2 }}>{eyebrow}</p>
          <h2 className="font-extrabold leading-[1.12] tracking-tight text-slate-900 dark:text-white" style={{ fontSize: 'clamp(1.7rem, 3.5vw, 2.5rem)' }}>
            {title}{' '}
            <span style={{ background: grad, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>{titleGradient}</span>
          </h2>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
          {stats.map((s, i) => (
            <div
              key={i}
              className="relative rounded-[20px] p-7 overflow-hidden cursor-default transition-all duration-250 group
                bg-white dark:bg-[#12121e]
                border border-slate-200 dark:border-white/[0.06]
                hover:border-slate-300 dark:hover:border-white/[0.15]
                hover:-translate-y-0.5"
            >
              {/* glow */}
              <div className="pointer-events-none absolute -top-14 -right-14 w-48 h-48 rounded-full opacity-50 group-hover:opacity-100 transition-opacity"
                style={{ background: s.glowColor, filter: 'blur(60px)' }} />
              {/* top line */}
              <div className="absolute top-0 left-8 right-8 h-px" style={{ background: `linear-gradient(90deg, transparent, ${s.color}, transparent)` }} />

              <div className="w-11 h-11 rounded-xl flex items-center justify-center text-xl mb-5"
                style={{ background: s.iconBg, border: `1px solid ${s.iconBorder}` }}>
                {s.icon}
              </div>
              <div className="text-[2.8rem] font-black leading-none tracking-tight mb-1" style={{ color: s.color }}>
                {s.numericTarget !== undefined
                  ? <AnimatedNumber target={s.numericTarget} suffix={s.suffix ?? ''} active={animated} />
                  : s.value}
              </div>
              <div className="text-[14px] font-semibold mb-1.5 text-slate-800 dark:text-white">{s.label}</div>
              <div className="text-[13px] leading-[1.6] text-slate-500 dark:text-[#8080a8]">{s.description}</div>
            </div>
          ))}

          {/* Wide card */}
          <div className="sm:col-span-2 relative rounded-[20px] p-7 overflow-hidden
            bg-white dark:bg-[#12121e]
            border border-slate-200 dark:border-white/[0.06]
            hover:border-slate-300 dark:hover:border-white/[0.15]
            transition-all duration-250 hover:-translate-y-0.5 group">
            <div className="pointer-events-none absolute -top-14 -right-14 w-48 h-48 rounded-full opacity-50 group-hover:opacity-100 transition-opacity"
              style={{ background: 'rgba(99,102,241,0.25)', filter: 'blur(60px)' }} />
            <div className="absolute top-0 left-8 right-8 h-px" style={{ background: `linear-gradient(90deg, transparent, ${accentColor}, transparent)` }} />

            <div className="flex flex-col md:flex-row gap-8 items-start">
              <div className="md:w-64 flex-shrink-0">
                <div className="w-11 h-11 rounded-xl flex items-center justify-center text-xl mb-4"
                  style={{ background: 'rgba(99,102,241,0.12)', border: '1px solid rgba(99,102,241,0.2)' }}>🔗</div>
                <div className="text-[16px] font-bold mb-2 text-slate-800 dark:text-white">{wideTitle}</div>
                <div className="text-[13px] leading-[1.6] text-slate-500 dark:text-[#8080a8]">{wideDescription}</div>
              </div>
              <div className="flex flex-wrap gap-2 flex-1">
                {wideBadges.map((b, i) => (
                  <span key={i} className={`text-[11px] font-semibold px-3 py-1.5 rounded-full border ${BADGE_STYLES[b.colorClass] ?? BADGE_STYLES.indigo}`}>
                    {b.text}
                  </span>
                ))}
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
