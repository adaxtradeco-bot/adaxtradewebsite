/**
 * Form Builder Final CTA Section
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 */

'use client';

import React from 'react';

interface FormBuilderFinalCTAProps {
  data: {
    title: string;
    description: string;
    buttonText: string;
    buttonHref: string;
  };
  style?: any;
}

export default function FormBuilderFinalCTASection({ data }: FormBuilderFinalCTAProps) {
  return (
    <section className="mx-auto max-w-7xl px-6 py-16">
      <div className="text-center">
        <h3 className="text-2xl font-bold text-slate-900 dark:text-white mb-2">{data.title}</h3>
        <p className="text-slate-600 dark:text-slate-300 mb-6">{data.description}</p>
        <a 
          href={data.buttonHref} 
          className="inline-block px-6 py-3 rounded-xl font-semibold text-white transition-opacity hover:opacity-90"
          style={{
            background: 'linear-gradient(90deg, #7c3aed, #06b6d4)'
          }}
        >
          {data.buttonText}
        </a>
      </div>
    </section>
  );
}