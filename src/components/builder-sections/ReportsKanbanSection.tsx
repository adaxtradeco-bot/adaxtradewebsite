/**
 * ReportsKanbanSection
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 */
'use client';
import React from 'react';

interface KanbanCard { title: string; assignee: string; due: string; dotColor: string; faded?: boolean; }
interface KanbanColumn { title: string; count: number; cards: KanbanCard[]; }

interface ReportsKanbanSectionData {
  eyebrow?: string;
  title?: string;
  titleGradient?: string;
  description?: string;
  columns?: KanbanColumn[];
  mediaUrl?: string;
  mediaObjectFit?: 'cover' | 'contain' | 'fill';
  accentColor?: string;
  accentColor2?: string;
}

interface Props {
  data: ReportsKanbanSectionData;
  style?: { backgroundColor?: string; textColor?: string; padding?: string };
  isBuilder?: boolean;
}

const DEFAULT_COLUMNS: KanbanColumn[] = [
  {
    title: 'To Do', count: 3,
    cards: [
      { title: 'Set up data pipeline', assignee: 'Ali K.', due: 'Due Thu', dotColor: '#6366f1' },
      { title: 'Design report template', assignee: 'Sara M.', due: 'Due Fri', dotColor: '#06b6d4' },
      { title: 'Review SLA thresholds', assignee: 'Nour R.', due: 'Due Mon', dotColor: '#8b5cf6' },
    ],
  },
  {
    title: 'In Progress', count: 2,
    cards: [
      { title: 'Connect SQL datasource', assignee: 'Omar S.', due: 'Due Today', dotColor: '#f59e0b' },
      { title: 'Build executive KPI dash', assignee: 'Dina H.', due: 'Due Fri', dotColor: '#10b981' },
    ],
  },
  {
    title: 'Done', count: 4,
    cards: [
      { title: 'User access setup', assignee: 'Completed', due: '', dotColor: '#10b981', faded: true },
      { title: 'Import historical data', assignee: 'Completed', due: '', dotColor: '#10b981', faded: true },
    ],
  },
];

export default function ReportsKanbanSection({ data, isBuilder = false }: Props) {
  const {
    eyebrow = 'Kanban boards',
    title = 'Workflow visibility —',
    titleGradient = 'at a glance',
    description = 'Drag and drop work items between stages. Track SLAs and priorities visually, without opening every record.',
    columns = DEFAULT_COLUMNS,
    mediaUrl = '',
    mediaObjectFit = 'contain',
    accentColor = '#6366f1',
    accentColor2 = '#818cf8',
  } = data;

  const grad = `linear-gradient(135deg, ${accentColor}, #8b5cf6, #06b6d4)`;
  const isVideo = (url: string) => ['.mp4', '.webm', '.ogg', '.mov'].some(e => url.toLowerCase().includes(e));

  const renderVisual = () => {
    if (mediaUrl) {
      if (isVideo(mediaUrl)) {
        return <video src={mediaUrl} className="w-full rounded-2xl" style={{ maxHeight: 360, objectFit: mediaObjectFit }} autoPlay muted loop playsInline />;
      }
      return <img src={mediaUrl} alt="Kanban preview" className="w-full rounded-2xl" style={{ maxHeight: 360, objectFit: mediaObjectFit }} />;
    }

    return (
      <div className="grid grid-cols-1 sm:grid-cols-3 gap-2.5">
        {columns.map((col, ci) => (
          <div key={ci} className="rounded-xl p-3 bg-white dark:bg-[#12121e] border border-slate-200 dark:border-white/[0.06]">
            <div className="flex items-center justify-between mb-2.5">
              <span className="text-[11px] font-semibold text-slate-500 dark:text-[#8080a8]">{col.title}</span>
              <span className="text-[10px] font-semibold px-1.5 py-0.5 rounded-full bg-slate-100 dark:bg-[#0e0e1a] text-slate-400 dark:text-[#404060]">{col.count}</span>
            </div>
            {col.cards.map((card, ki) => (
              <div
                key={ki}
                className="rounded-lg p-2.5 mb-1.5 cursor-pointer transition-colors border bg-slate-50 dark:bg-[#0e0e1a] border-slate-200 dark:border-white/[0.06] hover:border-indigo-300 dark:hover:border-indigo-500/40"
                style={{ opacity: card.faded ? 0.6 : 1 }}
              >
                <div className="text-[11px] font-medium mb-1 text-slate-800 dark:text-white">{card.title}</div>
                <div className="flex items-center gap-1.5 text-[10px] text-slate-400 dark:text-[#404060]">
                  <span className="w-1.5 h-1.5 rounded-full flex-shrink-0" style={{ background: card.dotColor }} />
                  {card.assignee}{card.due ? ` · ${card.due}` : ''}
                </div>
              </div>
            ))}
          </div>
        ))}
      </div>
    );
  };

  return (
    <section className="py-20 md:py-24 bg-white dark:bg-[#080810] border-t border-slate-200 dark:border-white/[0.06]">
      <div className="max-w-[1000px] mx-auto px-5 md:px-8">
        <p className="text-[11px] font-semibold tracking-[0.1em] uppercase mb-3" style={{ color: accentColor2 }}>{eyebrow}</p>
        <h2 className="font-extrabold leading-[1.12] tracking-tight mb-3 text-slate-900 dark:text-white" style={{ fontSize: 'clamp(1.7rem, 3.5vw, 2.5rem)' }}>
          {title}{' '}
          <span style={{ background: grad, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>{titleGradient}</span>
        </h2>
        <p className="text-[15px] leading-[1.7] text-slate-500 dark:text-[#8080a8] mb-8 max-w-[520px]">{description}</p>
        {renderVisual()}
      </div>
    </section>
  );
}
