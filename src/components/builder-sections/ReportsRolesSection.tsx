/**
 * ReportsRolesSection - Who Uses This
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 */
'use client';
import React from 'react';

interface RoleCard { title: string; description: string; accentColor?: string; }

interface ReportsRolesSectionData {
  eyebrow?: string;
  title?: string;
  titleGradient?: string;
  description?: string;
  roles?: RoleCard[];
  accentColor?: string;
  accentColor2?: string;
}

interface Props {
  data: ReportsRolesSectionData;
  style?: { backgroundColor?: string; textColor?: string; padding?: string };
  isBuilder?: boolean;
}

const DEFAULT_ROLES: RoleCard[] = [
  { title: 'Operations managers', description: 'Track processes, identify delays, and optimize workflows in real-time.', accentColor: '#6366f1' },
  { title: 'Team leads', description: 'Monitor workload, performance, and SLA compliance at a glance.', accentColor: '#06b6d4' },
  { title: 'Executives', description: 'High-level KPIs and business metrics for fast, informed decisions.', accentColor: '#8b5cf6' },
  { title: 'IT & automation teams', description: 'Debug workflows, track scripts, and monitor system health.', accentColor: '#10b981' },
  { title: 'External partners', description: 'Share read-only dashboards without exposing sensitive data.', accentColor: '#f59e0b' },
];

export default function ReportsRolesSection({ data, isBuilder = false }: Props) {
  const {
    eyebrow = 'Who uses this',
    title = 'Built for',
    titleGradient = 'the entire organization',
    description = 'One platform — every team, every role, every level of the business.',
    roles = DEFAULT_ROLES,
    accentColor = '#6366f1',
    accentColor2 = '#818cf8',
  } = data;

  const grad = `linear-gradient(135deg, ${accentColor}, #8b5cf6, #06b6d4)`;

  return (
    <section className="py-20 md:py-24 bg-white dark:bg-[#080810] border-t border-slate-200 dark:border-white/[0.06]">
      <div className="max-w-[1000px] mx-auto px-5 md:px-8">
        <p className="text-[11px] font-semibold tracking-[0.1em] uppercase mb-3" style={{ color: accentColor2 }}>{eyebrow}</p>
        <h2 className="font-extrabold leading-[1.12] tracking-tight mb-3 text-slate-900 dark:text-white" style={{ fontSize: 'clamp(1.7rem, 3.5vw, 2.5rem)' }}>
          {title}{' '}
          <span style={{ background: grad, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>{titleGradient}</span>
        </h2>
        <p className="text-[15px] leading-[1.7] text-slate-500 dark:text-[#8080a8] mb-8 max-w-[520px]">{description}</p>

        <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-2.5">
          {roles.map((r, i) => (
            <div
              key={i}
              className="rounded-2xl p-5 transition-transform duration-200 hover:-translate-y-0.5
                bg-white dark:bg-[#12121e]
                border border-slate-200 dark:border-white/[0.06]"
              style={{ borderTop: `3px solid ${r.accentColor ?? accentColor}` }}
            >
              <h3 className="text-[13px] font-bold mb-2 text-slate-800 dark:text-white">{r.title}</h3>
              <p className="text-[12px] leading-[1.6] text-slate-500 dark:text-[#8080a8]">{r.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
