/**
 * ReportsWhatYouCanBuildSection
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 */
'use client';
import React from 'react';

interface FeatureCard { icon: string; title: string; description: string; }

interface ReportsWhatYouCanBuildSectionData {
  eyebrow?: string;
  title?: string;
  titleGradient?: string;
  description?: string;
  features?: FeatureCard[];
  accentColor?: string;
  accentColor2?: string;
}

interface Props {
  data: ReportsWhatYouCanBuildSectionData;
  style?: { backgroundColor?: string; textColor?: string; padding?: string };
  isBuilder?: boolean;
}

const DEFAULT_FEATURES: FeatureCard[] = [
  { icon: '⚡', title: 'Real-time dashboards', description: 'Monitor your entire operation with live data that updates the moment something changes.' },
  { icon: '📊', title: 'Interactive charts', description: 'Bar, line, area, pie, scatter, heatmap and more. Zoom, filter, and explore with a few clicks.' },
  { icon: '⊞', title: 'Custom data tables', description: 'Sortable, filterable tables with inline editing, grouping, and conditional formatting.' },
  { icon: '▦', title: 'Kanban boards', description: 'Visualize tasks as interactive cards. Drag between stages and track progress at a glance.' },
  { icon: '📄', title: 'Automated reports', description: 'Schedule PDF or Excel reports and email them automatically to stakeholders.' },
  { icon: '✦', title: 'Smart filters & AI insights', description: 'Let AI detect patterns, anomalies, and bottlenecks automatically across all your data.' },
];

export default function ReportsWhatYouCanBuildSection({ data, isBuilder = false }: Props) {
  const {
    eyebrow = 'What you can build',
    title = 'Everything you need to',
    titleGradient = 'visualize your business',
    description = 'From executive dashboards to operational reports — build any view your team needs in minutes.',
    features = DEFAULT_FEATURES,
    accentColor = '#6366f1',
    accentColor2 = '#818cf8',
  } = data;

  const grad = `linear-gradient(135deg, ${accentColor}, #8b5cf6, #06b6d4)`;

  return (
    <section className="py-20 md:py-24 bg-white dark:bg-[#080810] border-t border-slate-200 dark:border-white/[0.06]">
      <div className="max-w-[1000px] mx-auto px-5 md:px-8">
        <p className="text-[11px] font-semibold tracking-[0.1em] uppercase mb-3" style={{ color: accentColor2 }}>{eyebrow}</p>
        <h2 className="font-extrabold leading-[1.12] tracking-tight mb-3 text-slate-900 dark:text-white" style={{ fontSize: 'clamp(1.7rem, 3.5vw, 2.5rem)' }}>
          {title}{' '}
          <span style={{ background: grad, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>
            {titleGradient}
          </span>
        </h2>
        <p className="text-[15px] leading-[1.7] text-slate-500 dark:text-[#8080a8] mb-10 max-w-[520px]">{description}</p>

        {/* Feature grid */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 divide-y sm:divide-y-0 divide-slate-200 dark:divide-white/[0.06] border border-slate-200 dark:border-white/[0.06] rounded-2xl overflow-hidden">
          {features.map((f, i) => (
            <div
              key={i}
              className="p-6 md:p-7 transition-colors duration-200
                bg-white dark:bg-[#12121e]
                hover:bg-slate-50 dark:hover:bg-[#181828]
                border-r-0 sm:border-r border-slate-200 dark:border-white/[0.06]
                last:border-r-0"
            >
              <div
                className="w-10 h-10 rounded-xl flex items-center justify-center text-lg mb-4"
                style={{ background: 'rgba(99,102,241,0.15)', border: '1px solid rgba(99,102,241,0.2)' }}
              >
                {f.icon}
              </div>
              <h3 className="text-[14px] font-semibold mb-1.5 text-slate-900 dark:text-white">{f.title}</h3>
              <p className="text-[13px] leading-[1.6] text-slate-500 dark:text-[#8080a8]">{f.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
