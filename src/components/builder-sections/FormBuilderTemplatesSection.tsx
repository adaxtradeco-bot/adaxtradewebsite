/**
 * Form Builder Templates Section - Horizontal Carousel
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 */

'use client';

import React, { useRef } from 'react';

interface FormBuilderTemplatesProps {
  data: {
    title: string;
    description: string;
    templates: Array<{
      title: string;
      description: string;
    }>;
  };
  style?: any;
}

export default function FormBuilderTemplatesSection({ data }: FormBuilderTemplatesProps) {
  const scrollRef = useRef<HTMLDivElement>(null);

  const scroll = (direction: 'left' | 'right') => {
    if (scrollRef.current) {
      const scrollAmount = 336; // card width + gap
      scrollRef.current.scrollBy({
        left: direction === 'left' ? -scrollAmount : scrollAmount,
        behavior: 'smooth'
      });
    }
  };

  return (
    <section className="mx-auto max-w-7xl px-6 py-16">
      <div className="flex items-end justify-between mb-6">
        <div>
          <h2 className="text-3xl md:text-4xl font-bold text-slate-900 dark:text-white">{data.title}</h2>
          <p className="mt-3 text-slate-600 dark:text-slate-300 max-w-3xl">{data.description}</p>
        </div>
        <div className="flex gap-2">
          <button 
            onClick={() => scroll('left')}
            className="px-3 py-2 rounded-lg bg-slate-100 dark:bg-white/10 hover:bg-slate-200 dark:hover:bg-white/15 text-slate-700 dark:text-white transition-colors border border-slate-200 dark:border-white/10"
          >
            ‹
          </button>
          <button 
            onClick={() => scroll('right')}
            className="px-3 py-2 rounded-lg bg-slate-100 dark:bg-white/10 hover:bg-slate-200 dark:hover:bg-white/15 text-slate-700 dark:text-white transition-colors border border-slate-200 dark:border-white/10"
          >
            ›
          </button>
        </div>
      </div>
      
      <div 
        ref={scrollRef}
        className="overflow-x-auto scrollbar-thin scrollbar-thumb-white/15 dark:scrollbar-thumb-white/15 scrollbar-thumb-slate-300 scrollbar-track-transparent"
        style={{ scrollbarWidth: 'thin' }}
      >
        <div className="flex gap-4 pb-4" style={{ minWidth: 'max-content' }}>
          {data.templates.map((template, index) => {
            const gradients = [
              'bg-gradient-to-br from-blue-50 to-blue-100 border-blue-200 text-blue-900',
              'bg-gradient-to-br from-emerald-50 to-emerald-100 border-emerald-200 text-emerald-900', 
              'bg-gradient-to-br from-violet-50 to-violet-100 border-violet-200 text-violet-900',
              'bg-gradient-to-br from-cyan-50 to-cyan-100 border-cyan-200 text-cyan-900',
              'bg-gradient-to-br from-amber-50 to-amber-100 border-amber-200 text-amber-900'
            ];
            const textColors = [
              'text-blue-700',
              'text-emerald-700',
              'text-violet-700', 
              'text-cyan-700',
              'text-amber-700'
            ];
            return (
              <div 
                key={index} 
                className={`w-80 rounded-xl border p-6 hover:transform hover:-translate-y-1 transition-all duration-300 hover:shadow-lg flex-shrink-0 shadow-sm lift dark:bg-white/5 dark:border-white/10 ${gradients[index % gradients.length]}`}
              >
                <div className="text-lg font-semibold mb-2 dark:text-white">{template.title}</div>
                <p className={`text-sm mt-1 dark:text-slate-300 ${textColors[index % textColors.length]}`}>{template.description}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
}