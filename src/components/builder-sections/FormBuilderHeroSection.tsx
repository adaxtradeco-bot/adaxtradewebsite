/**
 * Form Builder Hero Section - Exact replica of setiran formbuilder-light.html
 * @deprecated Use ProductHeroSection instead
 * This component is kept for backward compatibility only
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

'use client';

import { SectionConfig } from '@/lib/page-builder/section-schemas';

interface FormBuilderHeroData {
  badge?: string;
  title?: string;
  titleHighlight?: string;
  description?: string;
  primaryButton?: {
    text: string;
    href: string;
  };
  secondaryButton?: {
    text: string;
    href: string;
  };
  badges?: string[];
  mediaType?: string;
  canvasIcon?: string;
  canvasLabel?: string;
  features?: Array<{
    icon: string;
    label: string;
  }>;
}

interface FormBuilderHeroSectionProps {
  section: SectionConfig;
}

export function FormBuilderHeroSection({ section }: FormBuilderHeroSectionProps) {
  const data = section.data as FormBuilderHeroData;

  return (
    <>
      <style jsx>{`
        .bg-grid {
          background:
            radial-gradient(1200px 800px at 85% -20%, rgba(124,58,237,.08), transparent 45%),
            radial-gradient(900px 600px at -10% 120%, rgba(6,182,212,.08), transparent 55%),
            #f8fafc;
        }
        .pill {
          background: linear-gradient(90deg, #7c3aed, #06b6d4);
        }
        .lift {
          transition: transform .25s ease, box-shadow .25s ease;
          will-change: transform;
        }
        .lift:hover {
          transform: translateY(-6px);
          box-shadow: 0 12px 28px rgba(0,0,0,.12);
        }
        .badge {
          font-size: .75rem;
          padding: .35rem .6rem;
          border: 1px solid rgba(148,163,184,.3);
          border-radius: .65rem;
          background: rgba(255,255,255,.8);
          color: #475569;
        }
      `}</style>
      
      <header className="bg-grid">
        <section className="mx-auto max-w-7xl px-6 pt-16 pb-20 lg:pt-20">
          <div className="grid lg:grid-cols-[1.15fr_.85fr] gap-10 items-center">
            <div className="reveal">
              {data.badge && (
                <span className="inline-flex items-center gap-2 text-xs px-3 py-1 rounded-full bg-emerald-50 border border-emerald-200 text-emerald-700">
                  <span className="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
                  {data.badge}
                </span>
              )}
              <h1 className="mt-5 text-4xl md:text-5xl font-extrabold leading-tight text-slate-900">
                {data.title} <span className="text-violet-600">{data.titleHighlight}</span> — No Code.
              </h1>
              {data.description && (
                <p className="mt-5 text-slate-600 max-w-xl">
                  {data.description}
                </p>
              )}
              <div className="mt-8 flex flex-wrap items-center gap-4">
                {data.primaryButton && (
                  <a href={data.primaryButton.href} className="px-5 py-3 rounded-xl font-semibold pill text-white">
                    {data.primaryButton.text}
                  </a>
                )}
                {data.secondaryButton && (
                  <a href={data.secondaryButton.href} className="px-5 py-3 rounded-xl font-semibold border border-slate-300 text-slate-700 hover:bg-slate-50">
                    {data.secondaryButton.text}
                  </a>
                )}
              </div>
              {data.badges && data.badges.length > 0 && (
                <div className="mt-8 flex gap-3 text-xs">
                  {data.badges.map((badge, index) => (
                    <span key={index} className="badge">{badge}</span>
                  ))}
                </div>
              )}
            </div>
            <div className="reveal">
              <div className="rounded-3xl overflow-hidden p-5 shadow-lg border border-slate-200"
                   style={{
                     background: `
                       radial-gradient(900px 600px at 80% -20%, rgba(124,58,237,.06), transparent 50%),
                       radial-gradient(700px 500px at -10% 120%, rgba(6,182,212,.06), transparent 55%),
                       #ffffff
                     `
                   }}>
                <figure className="placeholder rounded-2xl overflow-hidden" style={{
                  border: '1px dashed rgba(148,163,184,.3)',
                  background: 'rgba(255,255,255,.5)'
                }}>
                  <div className="w-full h-64 bg-slate-100 flex items-center justify-center text-slate-500">
                    {data.canvasLabel || 'Form Canvas | Fields | Rules | Preview'}
                  </div>
                  <figcaption className="text-xs text-slate-500 p-2 border-t border-dashed border-slate-300">
                    Canvas + field palette, live preview and properties.
                  </figcaption>
                </figure>
                <div className="grid grid-cols-3 gap-3 mt-4">
                  {data.features?.map((feature, index) => (
                    <div key={index} className="lift rounded-xl bg-slate-50 border border-slate-200 p-3 text-center">
                      <div className="text-lg">{feature.icon}</div>
                      <div className="text-xs text-slate-600">{feature.label}</div>
                    </div>
                  )) || [
                    { icon: '🧩', label: 'Drag & Drop' },
                    { icon: '✅', label: 'Validations' },
                    { icon: '🔗', label: 'Integrations' }
                  ].map((feature, index) => (
                    <div key={index} className="lift rounded-xl bg-slate-50 border border-slate-200 p-3 text-center">
                      <div className="text-lg">{feature.icon}</div>
                      <div className="text-xs text-slate-600">{feature.label}</div>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </section>
      </header>
    </>
  );
}