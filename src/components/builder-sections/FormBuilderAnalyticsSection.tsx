/**
 * Form Builder Analytics Section - Masonry Layout
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 */

'use client';

import React from 'react';

interface FormBuilderAnalyticsProps {
  data: {
    title: string;
    description: string;
    features: Array<{
      title: string;
      description: string;
    }>;
  };
  style?: any;
}

export default function FormBuilderAnalyticsSection({ data }: FormBuilderAnalyticsProps) {
  return (
    <section className="mx-auto max-w-7xl px-6 py-16">
      <div className="text-center max-w-3xl mx-auto mb-10">
        <h2 className="text-3xl md:text-4xl font-bold text-slate-900 dark:text-white">{data.title}</h2>
        <p className="mt-3 text-slate-600 dark:text-slate-300">{data.description}</p>
      </div>
      
      <div className="columns-1 md:columns-2 xl:columns-3 gap-4 space-y-4">
        {data.features.map((feature, index) => {
          const gradients = [
            'bg-gradient-to-br from-rose-50 to-rose-100 border-rose-200 text-rose-900',
            'bg-gradient-to-br from-orange-50 to-orange-100 border-orange-200 text-orange-900',
            'bg-gradient-to-br from-teal-50 to-teal-100 border-teal-200 text-teal-900',
            'bg-gradient-to-br from-purple-50 to-purple-100 border-purple-200 text-purple-900'
          ];
          const textColors = [
            'text-rose-700',
            'text-orange-700', 
            'text-teal-700',
            'text-purple-700'
          ];
          return (
            <div 
              key={index} 
              className={`break-inside-avoid rounded-2xl border p-5 transition-colors hover:shadow-lg shadow-sm dark:bg-white/5 dark:border-white/10 dark:hover:bg-white/10 ${gradients[index % gradients.length]}`}
            >
              <h3 className="font-semibold mb-2 dark:text-white">{feature.title}</h3>
              <p className={`text-sm mt-2 dark:text-slate-300 ${textColors[index % textColors.length]}`}>{feature.description}</p>
            </div>
          );
        })}
      </div>
    </section>
  );
}