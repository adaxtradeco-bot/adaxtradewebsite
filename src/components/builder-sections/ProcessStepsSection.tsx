/**
 * ProcessStepsSection - BPMS Implementation Timeline
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 * Purpose: Vertical timeline with numbered steps, hover effects, scroll reveal,
 *          header stats, and a footer CTA. Supports dark/light mode.
 */

'use client';

import React, { useEffect, useRef } from 'react';

/* ─── Types ─── */
interface ProcessStep {
  eyebrow: string;
  badge: string;
  title: string;
  description: string;
  tags: string[];
}

interface HeaderStat {
  value: string;
  label: string;
}

interface ProcessStepsSectionData {
  eyebrow?: string;
  sectionTitle?: string;
  sectionTitleHighlight?: string;
  description?: string;
  stats?: HeaderStat[];
  steps?: ProcessStep[];
  footerTitle?: string;
  footerSubtitle?: string;
  footerButtonText?: string;
  footerButtonHref?: string;
  accentColor?: string;
  accentColor2?: string;
}

interface ProcessStepsSectionProps {
  data: ProcessStepsSectionData;
  style?: {
    backgroundColor?: string;
    textColor?: string;
    padding?: string;
  };
  isBuilder?: boolean;
}

const DEFAULT_STEPS: ProcessStep[] = [
  {
    eyebrow: 'Discovery',
    badge: 'Business Analysis Team',
    title: 'Understand before we build',
    description:
      'We immerse ourselves in your organization — mapping workflows, interviewing stakeholders, and uncovering the real bottlenecks. You get a clear project charter, risk plan, and delivery roadmap before any configuration begins.',
    tags: ['Stakeholder mapping', 'Process audit', 'Risk & scope definition', 'Delivery roadmap'],
  },
  {
    eyebrow: 'Design & Structure',
    badge: 'Process Architects + UX Team',
    title: 'Blueprint every workflow visually',
    description:
      'Our specialists design every workflow, form, and user journey directly inside the BPMS visual designer — no technical background needed. From citizen portals to operator dashboards, every screen is mapped and validated with your team before configuration begins.',
    tags: ['Visual workflow design', 'Form building', 'Org structure & roles', 'Role & permission planning', 'API connection mapping'],
  },
  {
    eyebrow: 'Configure & Connect',
    badge: 'Configuration + Integration Team',
    title: 'Your processes, configured and connected',
    description:
      'Our team configures every form, business rule, dashboard, and KPI directly through the BPMS visual interface. We connect your existing systems — HR, CRM, SMS gateways, single sign-on — using built-in connectors and a no-code API integration layer, with continuous testing at every milestone.',
    tags: ['Visual form builder', 'System integrations', 'KPI dashboards', 'No-code API connections'],
  },
  {
    eyebrow: 'Pilot & Go Live',
    badge: 'Launch & Training Team',
    title: 'Validate with real users, launch with confidence',
    description:
      'We run a controlled pilot with real users in your organization, gather feedback, fine-tune processes through the visual designer, and train your team on the platform. A structured go-live plan and rollback readiness ensure a smooth, monitored launch with zero disruption.',
    tags: ['Controlled pilot', 'User training', 'Go-live planning', '24/7 monitoring'],
  },
  {
    eyebrow: 'Scale & Evolve',
    badge: 'Optimization & Support Team',
    title: 'Your platform grows with your ambition',
    description:
      "Post-launch is where real value compounds. We expand to new processes, deepen integrations, build executive-level analytics dashboards, and continuously refine flows based on live user feedback — all through the platform's visual interface, with your team fully in control.",
    tags: ['New process rollout', 'SLA management', 'Executive dashboards', 'Ongoing optimization'],
  },
];

const DEFAULT_STATS: HeaderStat[] = [
  { value: '5', label: 'Structured phases' },
  { value: '100%', label: 'Team-led delivery' },
  { value: '24/7', label: 'Post-launch support' },
];

/* ─── Component ─── */
export default function ProcessStepsSection({
  data,
  style,
  isBuilder = false,
}: ProcessStepsSectionProps) {
  const {
    eyebrow = 'How we work',
    sectionTitle = 'From first call',
    sectionTitleHighlight = 'full operation',
    description = 'Most BPMS projects fail not because of the platform, but because of poor implementation. We bring specialized teams to every phase — so you\'re never alone navigating the journey from first process to full automation.',
    stats = DEFAULT_STATS,
    steps = DEFAULT_STEPS,
    footerTitle = 'Ready to automate your business?',
    footerSubtitle = 'Start with a free discovery session — no commitment required.',
    footerButtonText = 'Book a free session',
    footerButtonHref = '#',
    accentColor = '#4F7FFF',
    accentColor2 = '#7B5CFF',
  } = data;

  const stepsRef = useRef<HTMLDivElement>(null);

  /* Scroll-reveal (skip in builder to avoid observer issues) */
  useEffect(() => {
    if (isBuilder) {
      stepsRef.current?.querySelectorAll<HTMLElement>('.ps-step').forEach(el => {
        el.style.opacity = '1';
        el.style.transform = 'none';
      });
      return;
    }

    const stepEls = stepsRef.current?.querySelectorAll<HTMLElement>('.ps-step');
    if (!stepEls) return;

    const observer = new IntersectionObserver(
      entries => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const el = entry.target as HTMLElement;
            const idx = parseInt(el.dataset.idx ?? '0');
            setTimeout(() => {
              el.style.opacity = '1';
              el.style.transform = 'translateY(0)';
            }, idx * 120);
            observer.unobserve(el);
          }
        });
      },
      { threshold: 0.15 }
    );

    stepEls.forEach(el => observer.observe(el));
    return () => observer.disconnect();
  }, [isBuilder, steps]);

  const accent = accentColor;
  const accent2 = accentColor2;
  const gradientText = `linear-gradient(135deg, ${accent} 0%, ${accent2} 100%)`;
  const gradientBg = `linear-gradient(135deg, ${accent} 0%, ${accent2} 100%)`;

  return (
    <section className="relative py-24 overflow-hidden bg-white dark:bg-[#07080A]">
      {/* Grid background */}
      <div
        className="pointer-events-none absolute inset-0"
        style={{
          backgroundImage: `linear-gradient(rgba(79,127,255,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(79,127,255,0.04) 1px, transparent 1px)`,
          backgroundSize: '60px 60px',
        }}
      />
      {/* Top ambient glow */}
      <div
        className="pointer-events-none absolute -top-48 left-1/2 -translate-x-1/2 w-[700px] h-[500px] rounded-full"
        style={{ background: `radial-gradient(ellipse, rgba(79,127,255,0.07) 0%, transparent 70%)` }}
      />

      <div className="relative z-10 max-w-[1160px] mx-auto px-5 md:px-10">

        {/* ── Header ── */}
        <div className="grid grid-cols-1 md:grid-cols-2 gap-10 md:gap-16 items-end mb-20">
          {/* Left */}
          <div>
            <div className="inline-flex items-center gap-2.5 mb-7">
              <span
                className="block w-8 h-[1.5px] rounded"
                style={{ background: gradientBg }}
              />
              <span
                className="text-[11px] font-semibold tracking-[0.18em] uppercase"
                style={{ color: accent }}
              >
                {eyebrow}
              </span>
            </div>
            <h2 className="font-extrabold leading-[1.08] tracking-tight text-slate-900 dark:text-[#F0F2F8]"
              style={{ fontSize: 'clamp(34px, 5vw, 54px)' }}>
              {sectionTitle}
              <br />
              to{' '}
              <em
                className="not-italic"
                style={{
                  background: gradientText,
                  WebkitBackgroundClip: 'text',
                  WebkitTextFillColor: 'transparent',
                  backgroundClip: 'text',
                }}
              >
                {sectionTitleHighlight}
              </em>
            </h2>
          </div>

          {/* Right */}
          <div className="pb-1">
            <p className="text-[15px] leading-[1.75] font-light text-slate-500 dark:text-[#8A8FA8] mb-8">
              {description}
            </p>
            <div className="flex flex-wrap gap-6 md:gap-8">
              {stats.map((s, i) => (
                <div
                  key={i}
                  className="border-l-[1.5px] border-slate-300 dark:border-white/15 pl-4"
                >
                  <div className="font-bold text-[26px] leading-none mb-1 text-slate-900 dark:text-[#F0F2F8]">
                    {s.value}
                  </div>
                  <div className="text-[11px] tracking-wide text-slate-400 dark:text-[#4A4F65]">
                    {s.label}
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* ── Timeline ── */}
        <div ref={stepsRef} className="flex flex-col">
          {steps.map((step, idx) => (
            <div
              key={idx}
              data-idx={idx}
              className="ps-step group grid gap-x-9"
              style={{
                gridTemplateColumns: '68px 1fr',
                opacity: isBuilder ? 1 : 0,
                transform: isBuilder ? 'none' : 'translateY(30px)',
                transition: 'opacity 0.6s ease, transform 0.6s ease',
              }}
            >
              {/* Connector */}
              <div className="flex flex-col items-center flex-shrink-0">
                {/* Node */}
                <div
                  className="w-[68px] h-[68px] rounded-full flex items-center justify-center relative z-10 flex-shrink-0 transition-all duration-300
                    bg-slate-100 dark:bg-[#1A1D24]
                    border border-slate-200 dark:border-white/7
                    group-hover:border-[var(--ps-accent)] group-hover:bg-[rgba(79,127,255,0.1)]
                    group-hover:shadow-[0_0_0_6px_rgba(79,127,255,0.08)]"
                  style={{ '--ps-accent': accent } as React.CSSProperties}
                >
                  <span
                    className="font-extrabold text-[17px] transition-colors duration-300 text-slate-400 dark:text-[#4A4F65] group-hover:text-[var(--ps-accent)]"
                    style={{ '--ps-accent': accent } as React.CSSProperties}
                  >
                    {String(idx + 1).padStart(2, '0')}
                  </span>
                </div>
                {/* Line */}
                {idx < steps.length - 1 && (
                  <div className="w-[1.5px] flex-1 min-h-[40px] bg-gradient-to-b from-slate-300 dark:from-white/15 to-slate-200 dark:to-white/7" />
                )}
              </div>

              {/* Card */}
              <div
                className="relative overflow-hidden rounded-[20px] mb-5 transition-all duration-300
                  bg-slate-50 dark:bg-[#1A1D24]
                  border border-slate-200 dark:border-white/7
                  group-hover:border-slate-300 dark:group-hover:border-white/15
                  group-hover:bg-white dark:group-hover:bg-[#20242D]
                  group-hover:translate-x-1.5
                  group-hover:shadow-[0_20px_60px_rgba(0,0,0,0.08),0_0_0_1px_rgba(79,127,255,0.08)]
                  dark:group-hover:shadow-[0_20px_60px_rgba(0,0,0,0.35),0_0_0_1px_rgba(79,127,255,0.1)]"
                style={{ padding: '28px 32px' }}
              >
                {/* Top gradient line */}
                <div
                  className="absolute top-0 left-0 right-0 h-[1.5px] opacity-0 group-hover:opacity-100 transition-opacity duration-300"
                  style={{ background: `linear-gradient(90deg, transparent, ${accent}, ${accent2}, transparent)` }}
                />
                {/* Glow orb */}
                <div
                  className="pointer-events-none absolute -top-20 -right-20 w-64 h-64 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-300"
                  style={{ background: `radial-gradient(circle, rgba(79,127,255,0.12) 0%, transparent 70%)` }}
                />

                {/* Meta row */}
                <div className="flex flex-col gap-2 mb-4">
                  <div className="flex flex-wrap items-center gap-3">
                    <span
                      className="text-[11px] font-bold tracking-[0.2em] uppercase"
                      style={{ color: accent }}
                    >
                      {step.eyebrow}
                    </span>
                    <span
                      className="inline-flex items-center gap-1.5 text-[11px] font-medium rounded-full px-3 py-1.5 whitespace-nowrap transition-all duration-300
                        text-slate-500 dark:text-[#8A8FA8]
                        bg-white dark:bg-[#13151A]
                        border border-slate-200 dark:border-white/7
                        group-hover:border-slate-300 dark:group-hover:border-[rgba(79,127,255,0.3)]
                        group-hover:text-slate-700 dark:group-hover:text-[#F0F2F8]"
                    >
                      <span
                        className="w-1.5 h-1.5 rounded-full flex-shrink-0"
                        style={{
                          background: accent,
                          animation: 'ps-pulse 2.2s infinite',
                        }}
                      />
                      {step.badge}
                    </span>
                  </div>
                  <h3 className="font-extrabold text-[22px] md:text-[24px] leading-[1.2] tracking-tight text-slate-900 dark:text-[#F0F2F8]">
                    {step.title}
                  </h3>
                </div>

                <p className="text-[14px] leading-[1.72] font-light text-slate-500 dark:text-[#8A8FA8] mb-5">
                  {step.description}
                </p>

                {/* Tags */}
                <div className="flex flex-wrap gap-2">
                  {step.tags.map((tag, ti) => (
                    <span
                      key={ti}
                      className="text-[11.5px] rounded-md px-3 py-1 transition-all duration-200
                        text-slate-400 dark:text-[#4A4F65]
                        border border-slate-200 dark:border-white/7
                        group-hover:border-[rgba(79,127,255,0.25)]
                        group-hover:text-slate-500 dark:group-hover:text-[#8A8FA8]
                        group-hover:bg-[rgba(79,127,255,0.04)]"
                    >
                      {tag}
                    </span>
                  ))}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* ── Footer CTA ── */}
        <div
          className="relative overflow-hidden mt-16 rounded-3xl flex flex-col md:flex-row items-center justify-between gap-7 px-8 md:px-12 py-10
            bg-slate-50 dark:bg-[#1A1D24]
            border border-slate-200 dark:border-white/7"
        >
          <div
            className="pointer-events-none absolute inset-0"
            style={{
              background: `linear-gradient(135deg, rgba(79,127,255,0.04) 0%, rgba(123,92,255,0.04) 100%)`,
            }}
          />
          <div className="relative text-center md:text-left">
            <p className="font-bold text-[20px] text-slate-900 dark:text-[#F0F2F8] mb-1">
              {footerTitle}
            </p>
            <p className="text-[13.5px] font-light text-slate-500 dark:text-[#8A8FA8]">
              {footerSubtitle}
            </p>
          </div>
          <a
            href={footerButtonHref}
            className="relative inline-flex items-center gap-2.5 font-semibold text-[13.5px] text-white rounded-xl px-8 py-4 whitespace-nowrap transition-all duration-200 hover:-translate-y-0.5"
            style={{
              background: gradientBg,
              boxShadow: `0 8px 30px rgba(79,127,255,0.28)`,
            }}
            onMouseEnter={e => {
              (e.currentTarget as HTMLElement).style.boxShadow = `0 14px 40px rgba(79,127,255,0.4)`;
            }}
            onMouseLeave={e => {
              (e.currentTarget as HTMLElement).style.boxShadow = `0 8px 30px rgba(79,127,255,0.28)`;
            }}
          >
            {footerButtonText}
            <span className="text-lg transition-transform duration-200 group-hover:translate-x-1">→</span>
          </a>
        </div>
      </div>

      {/* Pulse keyframe */}
      <style>{`
        @keyframes ps-pulse {
          0%, 100% { opacity: 1; transform: scale(1); }
          50% { opacity: 0.5; transform: scale(0.7); }
        }
      `}</style>
    </section>
  );
}
