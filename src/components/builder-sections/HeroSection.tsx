/**
 * Builder Hero Section Component
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

import React from 'react';
import { Button } from '@/components/ui/Button';
import { HeroSectionConfig } from '@/lib/page-builder/section-schemas';

interface HeroSectionProps {
  section: HeroSectionConfig;
  isBuilder?: boolean;
}

export function HeroSection({ section, isBuilder = false }: HeroSectionProps) {
  const { data, style } = section;
  const isFullHero = style?.layout === 'full';

  if (isFullHero) {
    return (
      <section className="relative bg-gradient-to-br from-violet-50 via-slate-50 to-cyan-50 dark:from-violet-950 dark:via-slate-900 dark:to-cyan-950 text-slate-900 dark:text-white overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-r from-violet-200/30 via-transparent to-cyan-200/30 dark:from-violet-600/20 dark:via-transparent dark:to-cyan-500/20" />
        
        <div className="relative max-w-7xl mx-auto px-6 py-20 lg:py-32">
          <div className="grid lg:grid-cols-[1.1fr_0.9fr] gap-12 items-center">
            <div className="space-y-8">
              {data.subtitle && (
                <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white/10 dark:bg-white/10 bg-slate-900/10 border border-white/10 dark:border-white/10 border-slate-900/20 text-xs">
                  <span className="w-2 h-2 rounded-full bg-emerald-400 animate-pulse" />
                  {data.subtitle}
                </div>
              )}
              
              <h1 className="text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight">
                <span className="bg-gradient-to-r from-fuchsia-400 to-violet-400 bg-clip-text text-transparent">
                  {data.title}
                </span>
              </h1>
              
              <p className="text-lg text-slate-600 dark:text-slate-300 max-w-xl leading-relaxed">
                {data.description}
              </p>
              
              {data.buttons && data.buttons.length > 0 && (
                <div className="flex flex-wrap gap-4">
                  {data.buttons.map((button, index) => (
                    <Button
                      key={index}
                      variant={button.variant}
                      size={button.size || 'lg'}
                      className={button.variant === 'primary' 
                        ? 'bg-gradient-to-r from-violet-600 to-cyan-500 hover:from-violet-700 hover:to-cyan-600 text-white border-0' 
                        : 'border-white/20 dark:border-white/20 border-slate-900/20 text-white dark:text-white text-slate-900 hover:bg-white/10 dark:hover:bg-white/10 hover:bg-slate-900/10'}
                    >
                      {button.text}
                    </Button>
                  ))}
                </div>
              )}
              
              {data.badges && data.badges.length > 0 && (
                <div className="flex gap-3">
                  {data.badges.map((badge, index) => (
                    <span 
                      key={index}
                      className="px-3 py-1 text-xs rounded-full bg-white/10 dark:bg-white/10 bg-slate-900/10 border border-white/15 dark:border-white/15 border-slate-900/20 text-slate-300 dark:text-slate-300 text-slate-600"
                    >
                      {typeof badge === 'string' ? badge : badge.text}
                    </span>
                  ))}
                </div>
              )}
            </div>
            
            <div className="relative">
              <div className="rounded-3xl bg-gradient-to-br from-violet-600/20 to-cyan-500/20 dark:from-violet-600/20 dark:to-cyan-500/20 from-violet-200/40 to-cyan-200/40 p-6 border border-white/10 dark:border-white/10 border-slate-900/20 backdrop-blur-sm">
                <div className="aspect-[4/3] bg-slate-800/50 dark:bg-slate-800/50 bg-white/80 rounded-2xl border border-white/10 dark:border-white/10 border-slate-900/20 flex items-center justify-center mb-4">
                  <div className="text-center text-slate-400 dark:text-slate-400 text-slate-600">
                    <div className="text-4xl mb-2">🏗️</div>
                    <p className="text-sm">App Canvas | Forms | Tables | Rules</p>
                  </div>
                </div>
                
                <div className="grid grid-cols-3 gap-3">
                  {[
                    { icon: '🧩', label: 'Drag & Drop' },
                    { icon: '🔗', label: 'Integrations' },
                    { icon: '🛡️', label: 'Security' }
                  ].map((item) => (
                    <div 
                      key={item.label}
                      className="bg-white/5 dark:bg-white/5 bg-slate-900/5 border border-white/10 dark:border-white/10 border-slate-900/20 rounded-xl p-3 text-center hover:-translate-y-1 transition-transform"
                    >
                      <div className="text-lg mb-1">{item.icon}</div>
                      <div className="text-xs text-slate-300 dark:text-slate-300 text-slate-600">{item.label}</div>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    );
  }

  return (
    <section 
      className={`${style?.backgroundColor || 'bg-gradient-to-br from-slate-50 via-white to-slate-100/50'} 
                  ${style?.textColor || 'text-slate-900'} 
                  ${style?.padding || 'py-20'} 
                  dark:bg-slate-900 dark:text-white relative`}
    >
      <div className="max-w-7xl mx-auto px-6">
        <div className={`${style?.alignment === 'left' ? 'text-left' : 
                         style?.alignment === 'right' ? 'text-right' : 'text-center'} 
                        max-w-4xl ${style?.alignment === 'center' ? 'mx-auto' : ''}`}>
          
          {/* Badges */}
          {data.badges && data.badges.length > 0 && (
            <div className="flex flex-wrap gap-2 mb-6 justify-center">
              {data.badges.map((badge, index) => (
                <span
                  key={index}
                  className={`px-3 py-1 text-sm font-medium rounded-full 
                    ${badge.variant === 'success' ? 'bg-green-100 text-green-800 dark:bg-green-900/20 dark:text-green-400' :
                      badge.variant === 'info' ? 'bg-blue-100 text-blue-800 dark:bg-blue-900/20 dark:text-blue-400' :
                      'bg-gray-100 text-gray-800 dark:bg-gray-900/20 dark:text-gray-400'}`}
                >
                  {badge.text}
                </span>
              ))}
            </div>
          )}

          {/* Subtitle */}
          {data.subtitle && (
            <p className="text-lg text-slate-600 dark:text-slate-300 mb-4">
              {data.subtitle}
            </p>
          )}

          {/* Title */}
          <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
            {data.title}
          </h1>

          {/* Description */}
          <p className="text-xl text-slate-600 dark:text-slate-300 mb-8 leading-relaxed max-w-3xl mx-auto">
            {data.description}
          </p>

          {/* Buttons */}
          {data.buttons && data.buttons.length > 0 && (
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              {data.buttons.map((button, index) => (
                <Button
                  key={index}
                  variant={button.variant}
                  size={button.size || 'lg'}
                  className={`${button.variant === 'primary' 
                    ? 'bg-gradient-to-r from-violet-600 to-cyan-500 hover:from-violet-700 hover:to-cyan-600 text-white border-0 shadow-lg hover:shadow-xl' 
                    : ''}`}
                >
                  {button.text}
                </Button>
              ))}
            </div>
          )}
        </div>
      </div>

      {/* Background Pattern */}
      <div className="absolute inset-0 bg-[radial-gradient(circle_at_1px_1px,rgba(0,0,0,0.03)_1px,transparent_0)] dark:bg-none pointer-events-none" 
           style={{backgroundSize: '24px 24px'}} />
    </section>
  );
}