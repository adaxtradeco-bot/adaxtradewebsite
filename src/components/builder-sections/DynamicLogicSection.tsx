/**
 * Form Builder Dynamic Logic Section - Enhanced Professional Design
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 */

'use client';

import React from 'react';

interface DynamicLogicSectionProps {
  data: {
    title: string;
    description: string;
    features: Array<{
      title: string;
      description: string;
      icon: string;
    }>;
  };
  style?: any;
}

export default function DynamicLogicSection({ data }: DynamicLogicSectionProps) {
  return (
    <section className="bg-gradient-to-b from-violet-50 dark:from-violet-500/8 to-transparent">
      <div className="mx-auto max-w-7xl px-6 py-20">
        {/* Header */}
        <div className="text-center max-w-4xl mx-auto mb-16">
          <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-violet-100 dark:bg-violet-500/20 text-violet-700 dark:text-violet-300 text-sm font-semibold mb-6">
            <span className="w-2 h-2 rounded-full bg-violet-500 animate-pulse"></span>
            Smart Automation
          </div>
          <h2 className="text-4xl md:text-5xl font-bold text-slate-900 dark:text-white mb-6 leading-tight">
            {data.title}
          </h2>
          <p className="text-xl text-slate-600 dark:text-slate-300 leading-relaxed">
            {data.description}
          </p>
        </div>

        {/* Features Grid */}
        <div className="grid md:grid-cols-3 gap-8">
          {data.features.map((feature, index) => {
            const gradients = [
              'bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-500/10 dark:to-blue-600/10 border-blue-200 dark:border-blue-500/20',
              'bg-gradient-to-br from-emerald-50 to-emerald-100 dark:from-emerald-500/10 dark:to-emerald-600/10 border-emerald-200 dark:border-emerald-500/20',
              'bg-gradient-to-br from-violet-50 to-violet-100 dark:from-violet-500/10 dark:to-violet-600/10 border-violet-200 dark:border-violet-500/20'
            ];
            
            const iconColors = [
              'text-blue-600 bg-blue-100 dark:text-blue-400 dark:bg-blue-500/20',
              'text-emerald-600 bg-emerald-100 dark:text-emerald-400 dark:bg-emerald-500/20',
              'text-violet-600 bg-violet-100 dark:text-violet-400 dark:bg-violet-500/20'
            ];

            const textColors = [
              'text-blue-900 dark:text-blue-100',
              'text-emerald-900 dark:text-emerald-100',
              'text-violet-900 dark:text-violet-100'
            ];

            const descColors = [
              'text-blue-700 dark:text-blue-200',
              'text-emerald-700 dark:text-emerald-200',
              'text-violet-700 dark:text-violet-200'
            ];

            return (
              <div 
                key={index}
                className={`group relative rounded-3xl border p-8 hover:transform hover:-translate-y-2 transition-all duration-500 hover:shadow-2xl shadow-lg ${gradients[index % gradients.length]}`}
              >
                {/* Icon */}
                <div className={`w-16 h-16 rounded-2xl ${iconColors[index % iconColors.length]} flex items-center justify-center text-2xl mb-6 group-hover:scale-110 transition-transform duration-300`}>
                  {feature.icon}
                </div>

                {/* Content */}
                <h3 className={`text-2xl font-bold mb-4 ${textColors[index % textColors.length]}`}>
                  {feature.title}
                </h3>
                <p className={`text-lg leading-relaxed ${descColors[index % descColors.length]}`}>
                  {feature.description}
                </p>

                {/* Decorative Element */}
                <div className="absolute top-4 right-4 w-20 h-20 rounded-full bg-gradient-to-br from-white/20 to-white/5 dark:from-white/10 dark:to-white/5 opacity-50 group-hover:opacity-70 transition-opacity duration-300"></div>
              </div>
            );
          })}
        </div>

        {/* Bottom CTA */}
        <div className="text-center mt-16">
          <div className="inline-flex items-center gap-3 px-6 py-3 rounded-2xl bg-white dark:bg-white/10 border border-slate-200 dark:border-white/20 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
            <span className="text-slate-600 dark:text-slate-300 font-medium">Ready to add intelligence to your forms?</span>
            <span className="text-2xl">🚀</span>
          </div>
        </div>
      </div>
    </section>
  );
}