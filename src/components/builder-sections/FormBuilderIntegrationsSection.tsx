/**
 * Form Builder Integrations Section - Two Column with Diagram
 * Author: generated by Amazon Q
 * Created: 2024-12-21
 */

'use client';

import React from 'react';

interface FormBuilderIntegrationsProps {
  data: {
    title: string;
    description: string;
    integrations: string[];
    diagramAlt: string;
  };
  style?: any;
}

export default function FormBuilderIntegrationsSection({ data }: FormBuilderIntegrationsProps) {
  return (
    <section className="bg-gradient-to-b from-indigo-500/8 dark:from-indigo-500/8 from-blue-50 to-transparent">
      <div className="mx-auto max-w-7xl px-6 py-16 grid lg:grid-cols-2 gap-12 items-center">
        <div>
          <h2 className="text-3xl md:text-4xl font-bold text-slate-900 dark:text-white">{data.title}</h2>
          <p className="mt-3 text-slate-600 dark:text-slate-300">{data.description}</p>
          <ul className="mt-6 grid grid-cols-2 gap-3 text-sm">
            {data.integrations.map((integration, index) => {
              const colors = [
                'bg-blue-50 border-blue-200 text-blue-800',
                'bg-emerald-50 border-emerald-200 text-emerald-800',
                'bg-violet-50 border-violet-200 text-violet-800',
                'bg-cyan-50 border-cyan-200 text-cyan-800'
              ];
              return (
                <li key={index} className={`p-3 rounded-lg border hover:shadow-sm transition-colors dark:bg-white/5 dark:border-white/10 dark:hover:bg-white/10 dark:text-slate-300 ${colors[index % colors.length]}`}>
                  {integration}
                </li>
              );
            })}
          </ul>
        </div>
        <div>
          <figure className="rounded-2xl overflow-hidden border border-white/20 dark:border-white/20 border-slate-200 bg-white/3 dark:bg-white/3 bg-white shadow-lg">
            <img 
              src="https://placehold.co/1100x420/f1f5f9/475569?text=Integration+Diagram" 
              alt={data.diagramAlt}
              className="w-full h-auto"
            />
            <figcaption className="text-xs text-slate-400 dark:text-slate-400 text-slate-500 p-3 border-t border-white/15 dark:border-white/15 border-slate-200">
              Hub-and-spoke: Forms ↔ CRM/ERP/Email/Storage/Accounting via APIs & webhooks.
            </figcaption>
          </figure>
        </div>
      </div>
    </section>
  );
}