/**
 * Hero section component
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

import { Button } from './ui/Button';
import Link from 'next/link';
import { useParams } from 'next/navigation';
import { useLanguage } from '@/lib/i18n/context';

export function Hero() {
  const { language } = useLanguage();
  const params = useParams();
  const currentLang = params?.lang || language;
  return (
    <section className="bg-gradient-to-br from-primary-50 to-secondary-50 dark:from-neutral-900 dark:to-neutral-800 py-20 lg:py-32">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center">
          <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold text-neutral-900 dark:text-white mb-6">
            Master English with
            <span className="text-primary-600 dark:text-primary-400"> Confidence</span>
          </h1>
          <p className="text-lg sm:text-xl text-neutral-600 dark:text-neutral-300 mb-8 max-w-3xl mx-auto">
            Transform your English skills with our comprehensive learning platform. 
            Interactive lessons, personalized feedback, and real-world practice.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Button size="lg" asChild>
              <Link href={`/${currentLang}/contact`}>Start Learning</Link>
            </Button>
            <Button variant="outline" size="lg" asChild>
              <Link href={`/${currentLang}/form-builder`}>Explore Features</Link>
            </Button>
          </div>
        </div>
      </div>
    </section>
  );
}