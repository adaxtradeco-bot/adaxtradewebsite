/**
 * Hero section component with gradient controls
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

import { Button } from './ui/Button';
import Link from 'next/link';
import { useParams } from 'next/navigation';
import { useLanguage } from '@/lib/i18n/context';

interface GradientConfig {
  from: string;
  to: string;
  direction?: 'to-r' | 'to-br' | 'to-b' | 'to-bl' | 'to-l' | 'to-tl' | 'to-t' | 'to-tr';
}

interface HeroProps {
  backgroundGradient?: {
    light: GradientConfig;
    dark: GradientConfig;
  };
  titleGradient?: {
    light: GradientConfig;
    dark: GradientConfig;
  };
}

export function Hero({
  backgroundGradient = {
    light: { from: 'from-primary-50', to: 'to-secondary-50', direction: 'to-br' },
    dark: { from: 'from-neutral-900', to: 'to-neutral-800', direction: 'to-br' }
  },
  titleGradient = {
    light: { from: 'from-primary-600', to: 'to-secondary-600', direction: 'to-r' },
    dark: { from: 'from-primary-400', to: 'to-secondary-400', direction: 'to-r' }
  }
}: HeroProps) {
  const { language } = useLanguage();
  const params = useParams();
  const currentLang = params?.lang || language;

  const bgGradientClass = `bg-gradient-${backgroundGradient.light.direction} ${backgroundGradient.light.from} ${backgroundGradient.light.to} dark:${backgroundGradient.dark.from} dark:${backgroundGradient.dark.to}`;
  const titleGradientClass = `bg-gradient-${titleGradient.light.direction} ${titleGradient.light.from} ${titleGradient.light.to} dark:${titleGradient.dark.from} dark:${titleGradient.dark.to} bg-clip-text text-transparent`;

  return (
    <section className={`${bgGradientClass} py-20 lg:py-32`}>
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center">
          <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold mb-6">
            <span className="text-neutral-900 dark:text-white">Master English with</span>
            <span className={`block ${titleGradientClass}`}> Confidence</span>
          </h1>
          <p className="text-lg sm:text-xl text-neutral-600 dark:text-neutral-300 mb-8 max-w-3xl mx-auto">
            Transform your English skills with our comprehensive learning platform. 
            Interactive lessons, personalized feedback, and real-world practice.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Button size="lg" asChild>
              <Link href={`/${currentLang}/contact`}>Start Learning</Link>
            </Button>
            <Button variant="outline" size="lg" asChild>
              <Link href={`/${currentLang}/form-builder`}>Explore Features</Link>
            </Button>
          </div>
        </div>
      </div>
    </section>
  );
}