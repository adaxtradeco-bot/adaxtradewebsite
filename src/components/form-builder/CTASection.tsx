/**
 * Form Builder CTA Section - Final call-to-action with FAQ
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/Button';
import { useTranslation } from '@/hooks/useTranslation';

const faqData = [
  {
    category: 'gettingStarted',
    items: [
      {
        question: 'needCoding',
        answer: 'needCodingAnswer'
      },
      {
        question: 'howTemplates',
        answer: 'howTemplatesAnswer'
      }
    ]
  },
  {
    category: 'securityScaling', 
    items: [
      {
        question: 'enterpriseReady',
        answer: 'enterpriseReadyAnswer'
      },
      {
        question: 'canIntegrate',
        answer: 'canIntegrateAnswer'
      }
    ]
  }
];



interface CTASectionProps {
  glassEffect?: boolean;
}

export function CTASection({ glassEffect = false }: CTASectionProps) {
  const [openItems, setOpenItems] = useState<string[]>([]);
  const { t } = useTranslation();

  const toggleItem = (id: string) => {
    setOpenItems(prev => 
      prev.includes(id) 
        ? prev.filter(item => item !== id)
        : [...prev, id]
    );
  };

  return (
    <section className="bg-slate-50 dark:bg-slate-900 text-slate-900 dark:text-white py-16">
      <div className="max-w-7xl mx-auto px-6">
        {/* FAQ */}
        <div className="text-center max-w-3xl mx-auto mb-12">
          <h2 className="text-3xl md:text-4xl font-bold mb-4">{t('components.faq.title')}</h2>
          <p className="text-slate-600 dark:text-slate-300">
            {t('components.faq.subtitle')}
          </p>
        </div>
        
        <div className="grid lg:grid-cols-2 gap-6 mb-16">
          {faqData.map((section, sectionIndex) => (
            <div 
              key={sectionIndex}
              className={`rounded-2xl p-6 border transition-all ${
                glassEffect
                  ? 'bg-white/10 dark:bg-white/5 backdrop-blur-md border-white/20 dark:border-white/10 shadow-lg'
                  : 'bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 hover:shadow-md'
              }`}
            >
              <h3 className="font-semibold text-slate-900 dark:text-white mb-4">{t(`components.faq.${section.category}`)}</h3>
              
              <div className="space-y-1">
                {section.items.map((item, itemIndex) => {
                  const itemId = `${sectionIndex}-${itemIndex}`;
                  const isOpen = openItems.includes(itemId);
                  
                  return (
                    <div key={itemIndex} className="border-t border-slate-200 dark:border-slate-700">
                      <button
                        onClick={() => toggleItem(itemId)}
                        className="w-full text-left py-3 text-slate-700 dark:text-slate-300 hover:text-slate-900 dark:hover:text-white transition-colors font-medium"
                        aria-expanded={isOpen}
                      >
                        <span className="flex items-center justify-between">
                          {t(`components.faq.${item.question}`)}
                          <span className={`text-slate-400 dark:text-slate-500 transition-transform ${isOpen ? 'rotate-180' : ''}`}>
                            ↓
                          </span>
                        </span>
                      </button>
                      {isOpen && (
                        <div className="pb-3 text-sm text-slate-600 dark:text-slate-300">
                          {t(`components.faq.${item.answer}`)}
                        </div>
                      )}
                    </div>
                  );
                })}
              </div>
            </div>
          ))}
        </div>
        
        {/* CTA */}
        <div className="text-center bg-gradient-to-r from-fuchsia-600 to-pink-500 rounded-3xl p-8 border border-white/10 shadow-lg">
          <h3 className="text-2xl font-bold mb-2 text-white">{t('components.cta.title')}</h3>
          <p className="text-white/90 mb-8">
            {t('components.cta.description')}
          </p>
          <Button 
            size="lg"
            className="bg-white text-fuchsia-600 hover:bg-slate-100 border-0 shadow-lg hover:shadow-xl transition-all hover:scale-105"
          >
            {t('components.cta.secondaryButton')}
          </Button>
        </div>
      </div>
    </section>
  );
}