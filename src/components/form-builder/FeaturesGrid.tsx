/**
 * Form Builder Features Grid - Why choose enterprise form builder
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

'use client';

const features = [
  'Build in days, not months',
  'Multi-step, responsive layouts', 
  'Conditional logic & validations',
  'CRM/ERP/API integrations',
  'Audit trails & role-based access',
  'AI assistance for speed'
];

interface FeaturesGridProps {
  glassEffect?: boolean;
}

export function FeaturesGrid({ glassEffect = false }: FeaturesGridProps) {
  return (
    <section className="bg-slate-50 dark:bg-slate-900 text-slate-900 dark:text-white py-16">
      <div className="max-w-7xl mx-auto px-6">
        <div className="text-center max-w-3xl mx-auto mb-12">
          <h2 className="text-3xl md:text-4xl font-bold mb-4">
            Why Choose an Enterprise Form Builder?
          </h2>
          <p className="text-slate-600 dark:text-slate-300 text-lg leading-relaxed">
            Replace fragile spreadsheets and rigid off-the-shelf forms with tailored, 
            governed data collection that plugs into your systems. Ship faster, 
            standardize quality, and keep security tight.
          </p>
        </div>
        
        <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 max-w-4xl mx-auto">
          {features.map((feature, index) => (
            <div 
              key={index}
              className={`p-4 rounded-xl border transition-all hover:-translate-y-1 ${
                glassEffect
                  ? 'bg-white/10 dark:bg-white/5 backdrop-blur-md border-white/20 dark:border-white/10 shadow-lg'
                  : 'bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 hover:shadow-md'
              }`}
            >
              <p className="text-sm text-slate-700 dark:text-slate-300">{feature}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}