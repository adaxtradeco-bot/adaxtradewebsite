/**
 * Form Builder Fields Toggle - Interactive tabs for field types
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

'use client';

import { useState } from 'react';

type TabType = 'basics' | 'advanced' | 'appearance' | 'validation';

const tabContent = {
  basics: {
    title: 'Rich Field Library & Customization',
    description: 'Choose from 30+ field types—text, numbers, dates, files, signatures, maps and more. Add defaults, help text and options, then fine-tune visibility and roles.',
    features: [
      'Text, number, choice',
      'File & image upload', 
      'Date & time, signatures',
      'Maps & location'
    ]
  },
  advanced: {
    title: 'Advanced & Relational Fields',
    description: 'Capture complex data with computational fields, master-detail (child tables), API-connected lookups and nested forms. Import rows in bulk via CSV.',
    features: [
      'Computational fields',
      'Master-detail tables',
      'API lookups', 
      'Nested forms'
    ]
  },
  appearance: {
    title: 'Branding & Appearance',
    description: 'Apply your brand with themes and custom CSS. Group fields into sections, add icons or placeholders, and keep layouts clean and consistent.',
    features: [
      'Themes & CSS classes',
      'Field grouping',
      'Help text & hints',
      'Icons & placeholders'
    ]
  },
  validation: {
    title: 'Validation & Role-Based Visibility', 
    description: 'Validate inputs in real time and restrict visibility by role. Enforce patterns for email, phone and IDs, and toggle mandatory status dynamically.',
    features: [
      'Regex & pattern rules',
      'Mandatory/optional toggles',
      'Role-based visibility',
      'Inline error messages'
    ]
  }
};

export function FieldsToggle() {
  const [activeTab, setActiveTab] = useState<TabType>('basics');

  return (
    <section className="bg-slate-50 dark:bg-slate-900 text-slate-900 dark:text-white py-16">
      <div className="max-w-7xl mx-auto px-6">
        <div className="grid lg:grid-cols-[280px_1fr] gap-10">
          {/* Sidebar tabs */}
          <aside>
            <div className="rounded-2xl bg-white/5 dark:bg-white/5 bg-slate-900/5 border border-white/10 dark:border-white/10 border-slate-900/20 p-4 sticky top-6">
              {Object.keys(tabContent).map((tab) => (
                <button
                  key={tab}
                  onClick={() => setActiveTab(tab as TabType)}
                  className={`w-full text-left px-3 py-2 rounded-lg mb-2 transition-colors capitalize ${
                    activeTab === tab 
                      ? 'bg-white/10 dark:bg-white/10 bg-slate-900/10 text-white dark:text-white text-slate-900' 
                      : 'text-slate-300 dark:text-slate-300 text-slate-600 hover:bg-white/5 dark:hover:bg-white/5 hover:bg-slate-900/5 hover:text-white dark:hover:text-white hover:text-slate-900'
                  }`}
                >
                  {tab}
                </button>
              ))}
            </div>
          </aside>
          
          {/* Content */}
          <div className="rounded-2xl bg-white/5 dark:bg-white/5 bg-slate-900/5 border border-white/10 dark:border-white/10 border-slate-900/20 p-8">
            <h2 className="text-3xl font-bold mb-3">
              {tabContent[activeTab].title}
            </h2>
            <p className="text-slate-600 dark:text-slate-300 mb-6 leading-relaxed">
              {tabContent[activeTab].description}
            </p>
            
            <div className="grid sm:grid-cols-2 gap-4">
              {tabContent[activeTab].features.map((feature, index) => (
                <div 
                  key={index}
                  className="p-3 rounded-lg bg-white/5 dark:bg-white/5 bg-slate-900/5 border border-white/10 dark:border-white/10 border-slate-900/20 text-sm"
                >
                  {feature}
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}