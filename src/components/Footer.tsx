/**
 * Footer component
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

'use client';

import Link from 'next/link';
import { useTranslation } from '@/hooks/useTranslation';
import { useLanguage } from '@/lib/i18n/context';
import { useParams } from 'next/navigation';

export function Footer() {
  const { t } = useTranslation();
  const { language } = useLanguage();
  const params = useParams();
  const currentLang = params?.lang || language;
  return (
    <footer className="bg-slate-100 dark:bg-neutral-900 text-slate-600 dark:text-neutral-300 border-t border-slate-200 dark:border-neutral-800" role="contentinfo">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div className="grid md:grid-cols-3 gap-8">
          <div>
            <h3 className="text-slate-900 dark:text-white font-semibold mb-4">English Website</h3>
            <p className="text-sm leading-relaxed">
              {t('components.footer.about')}
            </p>
          </div>
          <div>
            <h4 className="text-slate-900 dark:text-white font-medium mb-4">{t('components.footer.product')}</h4>
            <nav className="space-y-2">
              <Link href={`/${currentLang}`} className="block text-sm hover:text-slate-900 dark:hover:text-white transition-colors">
                {t('nav.home')}
              </Link>
              <Link href={`/${currentLang}/form-builder`} className="block text-sm hover:text-slate-900 dark:hover:text-white transition-colors">
                {t('nav.product')}
              </Link>
              <Link href={`/${currentLang}/pricing`} className="block text-sm hover:text-slate-900 dark:hover:text-white transition-colors">
                {t('components.footer.pricing')}
              </Link>
              <Link href={`/${currentLang}/contact`} className="block text-sm hover:text-slate-900 dark:hover:text-white transition-colors">
                {t('components.footer.contact')}
              </Link>
            </nav>
          </div>
          <div>
            <h4 className="text-slate-900 dark:text-white font-medium mb-4">{t('components.footer.resources')}</h4>
            <div className="space-y-2 text-sm">
              <p>Email: hello@englishwebsite.com</p>
              <p>Phone: +1 (555) 123-4567</p>
            </div>
          </div>
        </div>
        <div className="border-t border-slate-200 dark:border-neutral-800 mt-8 pt-8 text-center text-sm">
          <p className="text-slate-500 dark:text-neutral-400">{t('components.footer.copyright')}</p>
        </div>
      </div>
    </footer>
  );
}