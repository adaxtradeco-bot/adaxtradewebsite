/**
 * Page Builder Canvas - Main canvas for drag-and-drop page building
 * Author: generated by Amazon Q
 * Created: 2024-01-15
 */

'use client';

import React, { memo, useCallback, useMemo } from 'react';
import { useSortable } from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';
import { SectionConfig } from '@/lib/page-builder/section-schemas';
import { SectionRenderer } from '@/lib/page-builder/section-renderer';
import { SectionCSSLoader } from '@/components/SectionCSSLoader';
import { Trash2, Copy, Grip, Palette } from 'lucide-react';

interface CanvasProps {
  sections: SectionConfig[];
  selectedSectionId: string | null;
  onSectionSelect: (sectionId: string) => void;
  onSectionDelete: (sectionId: string) => void;
  onSectionDuplicate: (sectionId: string) => void;
  previewMode: 'desktop' | 'tablet' | 'mobile';
  isPreview?: boolean;
  adminMode?: boolean;
}

export const Canvas = memo(function Canvas({
  sections,
  selectedSectionId,
  onSectionSelect,
  onSectionDelete,
  onSectionDuplicate,
  previewMode,
  isPreview = false,
  adminMode = false,
}: CanvasProps) {
  const getCanvasWidth = useCallback(() => {
    switch (previewMode) {
      case 'mobile':
        return 'max-w-sm mx-auto';
      case 'tablet':
        return 'max-w-2xl mx-auto';
      default:
        return 'w-full';
    }
  }, [previewMode]);

  const sortedSections = useMemo(() => 
    sections.sort((a, b) => a.order - b.order),
    [sections]
  );

  const handleSectionSelect = useCallback((sectionId: string) => {
    onSectionSelect(sectionId);
  }, [onSectionSelect]);

  const handleSectionDelete = useCallback((sectionId: string) => {
    onSectionDelete(sectionId);
  }, [onSectionDelete]);

  const handleSectionDuplicate = useCallback((sectionId: string) => {
    onSectionDuplicate(sectionId);
  }, [onSectionDuplicate]);

  return (
    <div className="flex-1 overflow-auto bg-gray-50 dark:bg-gray-900 p-6">
      <SectionCSSLoader sections={sections} />
      <div className={`${getCanvasWidth()} bg-white dark:bg-gray-800 shadow-2xl rounded-2xl overflow-hidden transition-all duration-300 border border-gray-200 dark:border-gray-700`}>
        {sections.length === 0 ? (
          <EmptyCanvas />
        ) : (
          <div className="relative">
            {/* Grid Background for sections */}
            <div className="absolute inset-0 opacity-20 pointer-events-none">
              <div className="h-full w-full" style={{
                backgroundImage: `
                  linear-gradient(rgba(0,0,0,0.03) 1px, transparent 1px),
                  linear-gradient(90deg, rgba(0,0,0,0.03) 1px, transparent 1px)
                `,
                backgroundSize: '24px 24px'
              }} />
            </div>
            
            <div className="relative space-y-0">
              {sortedSections.map((section) => (
                <SortableSection
                  key={section.id}
                  section={section}
                  isSelected={selectedSectionId === section.id}
                  onSelect={() => handleSectionSelect(section.id)}
                  onDelete={() => handleSectionDelete(section.id)}
                  onDuplicate={() => handleSectionDuplicate(section.id)}
                  isPreview={isPreview}
                  adminMode={adminMode}
                />
              ))}
            </div>
          </div>
        )}
      </div>
    </div>
  );
});

const EmptyCanvas = memo(function EmptyCanvas() {
  return (
    <div className="relative overflow-hidden bg-gradient-to-br from-gray-50 to-gray-100 dark:from-gray-800 dark:to-gray-900 rounded-xl">
      {/* Background Grid Pattern */}
      <div className="absolute inset-0 opacity-30">
        <div className="h-full w-full" style={{
          backgroundImage: `
            linear-gradient(rgba(0,0,0,0.05) 1px, transparent 1px),
            linear-gradient(90deg, rgba(0,0,0,0.05) 1px, transparent 1px)
          `,
          backgroundSize: '20px 20px'
        }} />
      </div>
      
      {/* Content */}
      <div className="relative p-20 text-center">
        <div className="w-24 h-24 mx-auto mb-6 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl flex items-center justify-center shadow-lg">
          <Palette className="w-12 h-12 text-white" />
        </div>
        <h3 className="text-2xl font-bold text-gray-900 dark:text-white mb-3">
          Start Building Your Page
        </h3>
        <p className="text-gray-600 dark:text-gray-400 max-w-md mx-auto leading-relaxed mb-8">
          Drag sections from the library on the left to start creating your page. You can customize each section and see changes in real-time.
        </p>
        
        {/* Feature Pills */}
        <div className="flex items-center justify-center space-x-4 text-sm">
          <div className="flex items-center space-x-2 px-3 py-2 bg-white dark:bg-gray-800 rounded-full shadow-sm border border-gray-200 dark:border-gray-700">
            <div className="w-2 h-2 bg-blue-500 rounded-full"></div>
            <span className="text-gray-700 dark:text-gray-300 font-medium">Drag & Drop</span>
          </div>
          <div className="flex items-center space-x-2 px-3 py-2 bg-white dark:bg-gray-800 rounded-full shadow-sm border border-gray-200 dark:border-gray-700">
            <div className="w-2 h-2 bg-green-500 rounded-full"></div>
            <span className="text-gray-700 dark:text-gray-300 font-medium">Real-time Preview</span>
          </div>
          <div className="flex items-center space-x-2 px-3 py-2 bg-white dark:bg-gray-800 rounded-full shadow-sm border border-gray-200 dark:border-gray-700">
            <div className="w-2 h-2 bg-purple-500 rounded-full"></div>
            <span className="text-gray-700 dark:text-gray-300 font-medium">Custom Styling</span>
          </div>
        </div>
      </div>
    </div>
  );
});

interface SortableSectionProps {
  section: SectionConfig;
  isSelected: boolean;
  onSelect: () => void;
  onDelete: () => void;
  onDuplicate: () => void;
  isPreview: boolean;
  adminMode?: boolean;
}

const SortableSection = memo(function SortableSection({
  section,
  isSelected,
  onSelect,
  onDelete,
  onDuplicate,
  isPreview,
  adminMode = false,
}: SortableSectionProps) {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
    isDragging,
  } = useSortable({ id: section.id });

  const style = useMemo(() => ({
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1,
  }), [transform, transition, isDragging]);

  const handleCSSEditor = useCallback((e: React.MouseEvent) => {
    e.stopPropagation();
    const event = new CustomEvent('openCSSEditor', { 
      detail: { 
        sectionId: section.id,
        sectionType: section.type,
        sectionOrder: section.order
      } 
    });
    window.dispatchEvent(event);
  }, [section.id, section.type, section.order]);

  const handleDuplicate = useCallback((e: React.MouseEvent) => {
    e.stopPropagation();
    onDuplicate();
  }, [onDuplicate]);

  const handleDelete = useCallback((e: React.MouseEvent) => {
    e.stopPropagation();
    onDelete();
  }, [onDelete]);

  return (
    <div
      ref={setNodeRef}
      style={style}
      className={`relative group transition-all duration-300 ${!isPreview ? 'cursor-pointer' : ''} ${
        isSelected && !isPreview 
          ? 'ring-2 ring-blue-500 ring-offset-4 ring-offset-white dark:ring-offset-gray-800' 
          : ''
      } ${
        isDragging ? 'z-50 shadow-2xl scale-105' : ''
      }`}
      onClick={!isPreview ? onSelect : undefined}
    >
      <SectionRenderer
        section={section}
        isBuilder={!isPreview}
        isSelected={isSelected}
        onSelect={onSelect}
        adminMode={adminMode && !isPreview}
      />

      {!isPreview && (
        <>
          {/* Drag Handle - Amazon Q Style */}
          <div
            {...attributes}
            {...listeners}
            className={`absolute top-4 left-4 p-3 bg-white dark:bg-gray-800 rounded-xl shadow-lg cursor-grab hover:bg-gray-50 dark:hover:bg-gray-700 transition-all duration-200 border border-gray-200 dark:border-gray-600 backdrop-blur-sm ${
              isSelected ? 'opacity-100 scale-100' : 'opacity-0 scale-90 group-hover:opacity-100 group-hover:scale-100'
            }`}
            title="Drag to reorder"
          >
            <Grip className="w-5 h-5 text-gray-600 dark:text-gray-400" />
          </div>

          {/* Action Buttons - Amazon Q Style */}
          <div className={`absolute top-4 right-4 flex space-x-2 transition-all duration-200 ${
            isSelected ? 'opacity-100 scale-100' : 'opacity-0 scale-90 group-hover:opacity-100 group-hover:scale-100'
          }`}>
            <button
              onClick={handleCSSEditor}
              className="p-3 bg-white dark:bg-gray-800 rounded-xl shadow-lg hover:bg-cyan-50 dark:hover:bg-cyan-900/20 text-cyan-600 dark:text-cyan-400 transition-all duration-200 border border-gray-200 dark:border-gray-600 hover:border-cyan-300 dark:hover:border-cyan-600 backdrop-blur-sm"
              title="Custom CSS"
            >
              <Palette className="w-5 h-5" />
            </button>
            <button
              onClick={handleDuplicate}
              className="p-3 bg-white dark:bg-gray-800 rounded-xl shadow-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-all duration-200 border border-gray-200 dark:border-gray-600 hover:border-gray-300 dark:hover:border-gray-500 backdrop-blur-sm"
              title="Duplicate Section"
            >
              <Copy className="w-5 h-5 text-gray-600 dark:text-gray-400" />
            </button>
            
            <button
              onClick={handleDelete}
              className="p-3 bg-white dark:bg-gray-800 rounded-xl shadow-lg hover:bg-red-50 dark:hover:bg-red-900/20 text-red-600 dark:text-red-400 transition-all duration-200 border border-gray-200 dark:border-gray-600 hover:border-red-300 dark:hover:border-red-600 backdrop-blur-sm"
              title="Delete Section"
            >
              <Trash2 className="w-5 h-5" />
            </button>
          </div>

          {/* Selection Indicator - Amazon Q Style */}
          {isSelected && (
            <>
              <div className="absolute inset-0 bg-blue-500/5 pointer-events-none rounded-lg" />
              <div className="absolute inset-0 border-2 border-blue-500/20 rounded-lg pointer-events-none" />
            </>
          )}
        </>
      )}
    </div>
  );
});
