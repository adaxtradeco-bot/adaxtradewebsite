/**
 * WhyAutomateWithUsPropertyPanel
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 * Purpose: Full visual editor for WhyAutomateWithUsSection
 */

'use client';

import React, { useState } from 'react';
import { SectionConfig } from '@/lib/page-builder/section-schemas';
import { MediaUpload } from '@/components/ui/MediaUpload';
import { IconButton, type IconConfig } from '@/components/ui/IconPicker';

interface WhyAutomateWithUsPropertyPanelProps {
  section: SectionConfig;
  onUpdate: (updates: Partial<SectionConfig>) => void;
}

/* ─── Reusable UI ─── */
const inputCls = 'w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-white dark:bg-gray-700 text-gray-900 dark:text-white text-sm';
const textareaCls = inputCls + ' resize-none';

function Field({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div>
      <label className="block text-xs font-medium text-gray-600 dark:text-gray-400 mb-1">{label}</label>
      {children}
    </div>
  );
}

function Collapsible({ title, badge, children, defaultOpen = false }: {
  title: string; badge?: string; children: React.ReactNode; defaultOpen?: boolean;
}) {
  const [open, setOpen] = useState(defaultOpen);
  return (
    <div className="border border-gray-200 dark:border-gray-600 rounded-lg overflow-hidden">
      <button type="button" onClick={() => setOpen(o => !o)}
        className="w-full flex items-center justify-between px-3 py-2.5 bg-gray-50 dark:bg-gray-700 hover:bg-gray-100 dark:hover:bg-gray-600 transition-colors text-left">
        <span className="text-sm font-medium text-gray-800 dark:text-white flex items-center gap-2">
          {title}
          {badge && <span className="text-xs bg-blue-100 dark:bg-blue-900/40 text-blue-700 dark:text-blue-300 px-2 py-0.5 rounded-full">{badge}</span>}
        </span>
        <span className={`text-gray-400 text-xs transition-transform ${open ? 'rotate-90' : ''}`}>▶</span>
      </button>
      {open && <div className="p-3 space-y-3 bg-white dark:bg-gray-800">{children}</div>}
    </div>
  );
}

/* ─── Card Editor ─── */
function CardEditor({ card, onChange, onRemove, onMoveUp, onMoveDown, isFirst, isLast }: {
  card: any; onChange: (c: any) => void; onRemove: () => void;
  onMoveUp: () => void; onMoveDown: () => void; isFirst: boolean; isLast: boolean;
}) {
  const updateStat = (i: number, field: string, val: string) => {
    const stats = [...(card.stats ?? [])];
    stats[i] = { ...stats[i], [field]: val };
    onChange({ ...card, stats });
  };
  const addStat = () => onChange({ ...card, stats: [...(card.stats ?? []), { value: '', label: '' }] });
  const removeStat = (i: number) => onChange({ ...card, stats: (card.stats ?? []).filter((_: any, idx: number) => idx !== i) });

  const updateAvatar = (i: number, field: string, val: string) => {
    const avatars = [...(card.avatars ?? [])];
    avatars[i] = { ...avatars[i], [field]: val };
    onChange({ ...card, avatars });
  };
  const addAvatar = () => onChange({ ...card, avatars: [...(card.avatars ?? []), { initials: 'XX', color: '#4F7FFF', bg: 'rgba(79,127,255,0.15)' }] });
  const removeAvatar = (i: number) => onChange({ ...card, avatars: (card.avatars ?? []).filter((_: any, idx: number) => idx !== i) });

  return (
    <Collapsible title={card.title || '(untitled)'}>
      <div className="flex gap-1 mb-1">
        <button type="button" onClick={onMoveUp} disabled={isFirst} className="px-2 py-1 text-xs bg-gray-100 dark:bg-gray-700 rounded disabled:opacity-30">↑</button>
        <button type="button" onClick={onMoveDown} disabled={isLast} className="px-2 py-1 text-xs bg-gray-100 dark:bg-gray-700 rounded disabled:opacity-30">↓</button>
        <button type="button" onClick={onRemove} className="ml-auto px-2 py-1 text-xs bg-red-100 dark:bg-red-900/30 text-red-600 rounded">🗑 Remove</button>
      </div>

      <Field label="Label (eyebrow)">
        <input className={inputCls} value={card.label ?? ''} onChange={e => onChange({ ...card, label: e.target.value })} />
      </Field>
      <Field label="Title">
        <input className={inputCls} value={card.title ?? ''} onChange={e => onChange({ ...card, title: e.target.value })} />
      </Field>
      <Field label="Description">
        <textarea className={textareaCls} rows={3} value={card.description ?? ''} onChange={e => onChange({ ...card, description: e.target.value })} />
      </Field>

      {/* Icon - New: FontAwesome Picker */}
      <Collapsible title="Icon" badge={card.iconConfig ? 'FA' : card.iconSvg ? 'SVG' : 'None'}>
        <Field label="FontAwesome Icon (Recommended)">
          <IconButton
            value={card.iconConfig}
            onChange={(icon: IconConfig | null) => onChange({ ...card, iconConfig: icon })}
            placeholder="Select FontAwesome icon"
          />
        </Field>
        
        <div className="text-xs text-gray-500 dark:text-gray-400 text-center py-2">— OR —</div>
        
        <Field label="Legacy: Icon SVG (paste raw SVG)">
          <textarea 
            className={textareaCls} 
            rows={3} 
            value={card.iconSvg ?? ''} 
            onChange={e => onChange({ ...card, iconSvg: e.target.value })} 
            placeholder="<svg viewBox='0 0 24 24'>...</svg>"
          />
        </Field>
        
        <Field label="Icon Background (rgba)">
          <input 
            className={inputCls} 
            value={card.iconBg ?? ''} 
            placeholder="rgba(79,127,255,0.08)" 
            onChange={e => onChange({ ...card, iconBg: e.target.value })} 
          />
        </Field>
        
        {!card.iconConfig && (
          <Field label="Icon Color (for SVG only)">
            <div className="flex gap-2">
              <input 
                type="color" 
                value={card.iconColor ?? '#4F7FFF'} 
                onChange={e => onChange({ ...card, iconColor: e.target.value })}
                className="w-10 h-9 rounded border border-gray-300 dark:border-gray-600 cursor-pointer p-0.5 bg-white dark:bg-gray-700" 
              />
              <input 
                className={inputCls} 
                value={card.iconColor ?? '#4F7FFF'} 
                onChange={e => onChange({ ...card, iconColor: e.target.value })} 
              />
            </div>
          </Field>
        )}
      </Collapsible>

      {/* Team card toggle */}
      <label className="flex items-center gap-2 text-sm text-gray-700 dark:text-gray-300 cursor-pointer">
        <input type="checkbox" checked={!!card.isTeamCard} onChange={e => onChange({ ...card, isTeamCard: e.target.checked })} />
        Team card style (blue gradient bg)
      </label>

      {/* Avatars */}
      <Collapsible title="Avatars" badge={`${(card.avatars ?? []).length}`}>
        {(card.avatars ?? []).map((av: any, i: number) => (
          <div key={i} className="flex gap-2 items-center">
            <input className={inputCls} placeholder="Initials" value={av.initials} onChange={e => updateAvatar(i, 'initials', e.target.value)} style={{ maxWidth: 70 }} />
            <input type="color" value={av.color} onChange={e => updateAvatar(i, 'color', e.target.value)} className="w-9 h-9 rounded border border-gray-300 dark:border-gray-600 cursor-pointer p-0.5" title="Text color" />
            <input className={inputCls} placeholder="bg rgba" value={av.bg} onChange={e => updateAvatar(i, 'bg', e.target.value)} />
            <button type="button" onClick={() => removeAvatar(i)} className="text-red-500 px-1">✕</button>
          </div>
        ))}
        <Field label="Extra label (e.g. +5)">
          <input className={inputCls} value={card.avatarExtra ?? ''} onChange={e => onChange({ ...card, avatarExtra: e.target.value })} />
        </Field>
        <button type="button" onClick={addAvatar} className="w-full py-1.5 border border-dashed border-gray-300 dark:border-gray-600 rounded text-xs text-gray-500 hover:border-blue-400 hover:text-blue-500">+ Add Avatar</button>
      </Collapsible>

      {/* Stats */}
      <Collapsible title="Stats" badge={`${(card.stats ?? []).length}`}>
        {(card.stats ?? []).map((s: any, i: number) => (
          <div key={i} className="flex gap-2 items-center">
            <input className={inputCls} placeholder="Value" value={s.value} onChange={e => updateStat(i, 'value', e.target.value)} style={{ maxWidth: 80 }} />
            <input className={inputCls} placeholder="Label" value={s.label} onChange={e => updateStat(i, 'label', e.target.value)} />
            <button type="button" onClick={() => removeStat(i)} className="text-red-500 px-1">✕</button>
          </div>
        ))}
        <button type="button" onClick={addStat} className="w-full py-1.5 border border-dashed border-gray-300 dark:border-gray-600 rounded text-xs text-gray-500 hover:border-blue-400 hover:text-blue-500">+ Add Stat</button>
      </Collapsible>

      {/* Media */}
      <Collapsible title="Media Slot">
        <Field label="Media Position">
          <select className={inputCls} value={card.mediaPosition ?? 'top'} onChange={e => onChange({ ...card, mediaPosition: e.target.value })}>
            <option value="top">Top</option>
            <option value="bottom">Bottom</option>
          </select>
        </Field>
        <Field label="Badge text">
          <input className={inputCls} value={card.media?.badge ?? ''} onChange={e => onChange({ ...card, media: { ...(card.media ?? {}), badge: e.target.value } })} />
        </Field>
        <Field label="Placeholder label">
          <input className={inputCls} value={card.media?.placeholderLabel ?? ''} onChange={e => onChange({ ...card, media: { ...(card.media ?? {}), placeholderLabel: e.target.value } })} />
        </Field>
        <Field label="Height (px)">
          <input type="number" className={inputCls} value={card.media?.height ?? 200} onChange={e => onChange({ ...card, media: { ...(card.media ?? {}), height: parseInt(e.target.value) || 200 } })} />
        </Field>
        <Field label="Media (image or video)">
          <MediaUpload
            src={card.media?.src ?? ''}
            alt={card.title}
            width={280}
            height={card.media?.height ?? 200}
            onMediaChange={(src: string) => onChange({ ...card, media: { ...(card.media ?? {}), src } })}
            objectFit={card.media?.objectFit ?? 'cover'}
            onObjectFitChange={(fit: string) => onChange({ ...card, media: { ...(card.media ?? {}), objectFit: fit } })}
            showObjectFitControls
            showUploadButton
            uploadButtonText="Upload Image / Video"
            acceptedTypes="image/*,video/*"
          />
        </Field>
        <button type="button" onClick={() => onChange({ ...card, media: undefined })}
          className="w-full py-1.5 text-xs text-red-500 border border-dashed border-red-300 dark:border-red-800 rounded hover:bg-red-50 dark:hover:bg-red-900/20">
          Remove media slot
        </button>
      </Collapsible>
    </Collapsible>
  );
}

/* ─── Main Panel ─── */
export function WhyAutomateWithUsPropertyPanel({ section, onUpdate }: WhyAutomateWithUsPropertyPanelProps) {
  const data = section.data as any;
  const update = (patch: Record<string, any>) => onUpdate({ data: { ...data, ...patch } });

  const updateCardInRow = (rowKey: string, idx: number, card: any) => {
    const row = [...(data[rowKey] ?? [])];
    row[idx] = card;
    update({ [rowKey]: row });
  };
  const removeCardFromRow = (rowKey: string, idx: number) => {
    update({ [rowKey]: (data[rowKey] ?? []).filter((_: any, i: number) => i !== idx) });
  };
  const moveCard = (rowKey: string, idx: number, dir: -1 | 1) => {
    const row = [...(data[rowKey] ?? [])];
    const t = idx + dir;
    if (t < 0 || t >= row.length) return;
    [row[idx], row[t]] = [row[t], row[idx]];
    update({ [rowKey]: row });
  };
  const addCard = (rowKey: string) => {
    update({
      [rowKey]: [...(data[rowKey] ?? []), {
        id: `card-${Date.now()}`, label: 'New Feature', title: 'Card Title',
        description: 'Card description.', iconBg: 'rgba(79,127,255,0.08)', iconColor: '#4F7FFF',
      }],
    });
  };

  const updateTicker = (idx: number, val: string) => {
    const items = [...(data.tickerItems ?? [])];
    items[idx] = { text: val };
    update({ tickerItems: items });
  };
  const addTicker = () => update({ tickerItems: [...(data.tickerItems ?? []), { text: '' }] });
  const removeTicker = (idx: number) => update({ tickerItems: (data.tickerItems ?? []).filter((_: any, i: number) => i !== idx) });

  const renderRowEditor = (rowKey: string, label: string) => {
    const cards: any[] = data[rowKey] ?? [];
    return (
      <Collapsible title={label} badge={`${cards.length} cards`}>
        <div className="space-y-2">
          {cards.map((card: any, idx: number) => (
            <CardEditor
              key={card.id ?? idx}
              card={card}
              onChange={c => updateCardInRow(rowKey, idx, c)}
              onRemove={() => removeCardFromRow(rowKey, idx)}
              onMoveUp={() => moveCard(rowKey, idx, -1)}
              onMoveDown={() => moveCard(rowKey, idx, 1)}
              isFirst={idx === 0}
              isLast={idx === cards.length - 1}
            />
          ))}
        </div>
        <button type="button" onClick={() => addCard(rowKey)}
          className="w-full py-2 border border-dashed border-blue-300 dark:border-blue-700 rounded-md text-sm text-blue-600 dark:text-blue-400 hover:bg-blue-50 dark:hover:bg-blue-900/20 font-medium mt-2">
          + Add Card
        </button>
      </Collapsible>
    );
  };

  return (
    <div className="space-y-4">

      {/* Header */}
      <Collapsible title="Section Header" defaultOpen>
        <Field label="Eyebrow"><input className={inputCls} value={data.eyebrow ?? ''} onChange={e => update({ eyebrow: e.target.value })} /></Field>
        <Field label="Title (before highlight)"><input className={inputCls} value={data.title ?? ''} onChange={e => update({ title: e.target.value })} /></Field>
        <Field label="Title Highlight (gradient)"><input className={inputCls} value={data.titleHighlight ?? ''} onChange={e => update({ titleHighlight: e.target.value })} /></Field>
        <Field label="Subtitle"><textarea className={textareaCls} rows={2} value={data.subtitle ?? ''} onChange={e => update({ subtitle: e.target.value })} /></Field>
      </Collapsible>

      {/* Colors */}
      <Collapsible title="Accent Colors">
        {(['accentColor', 'accentColor2', 'accentColor3'] as const).map((key, i) => (
          <Field key={key} label={['Primary', 'Secondary', 'Tertiary'][i] + ' Accent'}>
            <div className="flex gap-2">
              <input type="color" value={(data[key] as string) ?? '#4F7FFF'} onChange={e => update({ [key]: e.target.value })}
                className="w-10 h-9 rounded border border-gray-300 dark:border-gray-600 cursor-pointer p-0.5 bg-white dark:bg-gray-700" />
              <input className={inputCls} value={(data[key] as string) ?? ''} onChange={e => update({ [key]: e.target.value })} />
            </div>
          </Field>
        ))}
      </Collapsible>

      {/* Layout */}
      <Collapsible title="Layout & Spacing">
        <Field label="Section Padding (Tailwind class)">
          <select className={inputCls} value={data.sectionPaddingY ?? 'py-32'} onChange={e => update({ sectionPaddingY: e.target.value })}>
            <option value="py-16">Small (py-16)</option>
            <option value="py-20">Medium (py-20)</option>
            <option value="py-24">Large (py-24)</option>
            <option value="py-32">Extra Large (py-32)</option>
            <option value="py-40">2XL (py-40)</option>
          </select>
        </Field>
      </Collapsible>

      {/* Rows */}
      {renderRowEditor('row1Cards', 'Row 1 — Featured (2 cols)')}
      {renderRowEditor('row2Cards', 'Row 2 — Grid (3 cols)')}
      {renderRowEditor('row3Cards', 'Row 3 — Bottom (2 cols)')}

      {/* Ticker */}
      <Collapsible title="Ticker / Marquee" badge={`${(data.tickerItems ?? []).length} items`}>
        <Field label="Speed (seconds for full loop)">
          <input type="number" className={inputCls} value={data.tickerSpeed ?? 30} min={5} max={120}
            onChange={e => update({ tickerSpeed: parseInt(e.target.value) || 30 })} />
        </Field>
        <div className="space-y-1.5 max-h-60 overflow-y-auto">
          {(data.tickerItems ?? []).map((item: any, i: number) => (
            <div key={i} className="flex gap-2">
              <input className={inputCls} value={item.text ?? ''} onChange={e => updateTicker(i, e.target.value)} placeholder="Ticker text" />
              <button type="button" onClick={() => removeTicker(i)} className="text-red-500 px-1 text-sm">✕</button>
            </div>
          ))}
        </div>
        <button type="button" onClick={addTicker}
          className="w-full py-1.5 border border-dashed border-gray-300 dark:border-gray-600 rounded text-xs text-gray-500 hover:border-blue-400 hover:text-blue-500">
          + Add Ticker Item
        </button>
      </Collapsible>

    </div>
  );
}
