/**
 * IndustriesGridPropertyPanel
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 * Purpose: Full visual editor for IndustriesGridSection fields
 */

'use client';

import React, { useState } from 'react';
import { SectionConfig } from '@/lib/page-builder/section-schemas';

interface Props {
  section: SectionConfig;
  onUpdate: (updates: Partial<SectionConfig>) => void;
}

function Field({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div>
      <label className="block text-xs font-medium text-gray-600 dark:text-gray-400 mb-1">{label}</label>
      {children}
    </div>
  );
}

const inputCls = 'w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-white dark:bg-gray-700 text-gray-900 dark:text-white text-sm';
const textareaCls = inputCls + ' resize-none';

function Collapsible({ title, badge, children, defaultOpen = false }: {
  title: string; badge?: string; children: React.ReactNode; defaultOpen?: boolean;
}) {
  const [open, setOpen] = useState(defaultOpen);
  return (
    <div className="border border-gray-200 dark:border-gray-600 rounded-lg overflow-hidden">
      <button type="button" onClick={() => setOpen(o => !o)}
        className="w-full flex items-center justify-between px-3 py-2.5 bg-gray-50 dark:bg-gray-700 hover:bg-gray-100 dark:hover:bg-gray-600 transition-colors text-left">
        <span className="text-sm font-medium text-gray-800 dark:text-white flex items-center gap-2">
          {title}
          {badge && <span className="text-xs bg-blue-100 dark:bg-blue-900/40 text-blue-700 dark:text-blue-300 px-2 py-0.5 rounded-full">{badge}</span>}
        </span>
        <span className={`text-gray-400 text-xs transition-transform ${open ? 'rotate-90' : ''}`}>▶</span>
      </button>
      {open && <div className="p-3 space-y-3 bg-white dark:bg-gray-800">{children}</div>}
    </div>
  );
}

export function IndustriesGridPropertyPanel({ section, onUpdate }: Props) {
  const data = section.data as any;
  const update = (patch: Record<string, any>) => onUpdate({ data: { ...data, ...patch } });

  const industries = data.industries ?? [];
  const moreIndustries = data.moreIndustries ?? [];

  const updateIndustry = (idx: number, field: string, val: string) => {
    const next = industries.map((ind: any, i: number) => i === idx ? { ...ind, [field]: val } : ind);
    update({ industries: next });
  };

  const updateMore = (idx: number, field: string, val: string) => {
    const next = moreIndustries.map((m: any, i: number) => i === idx ? { ...m, [field]: val } : m);
    update({ moreIndustries: next });
  };

  return (
    <div className="space-y-4">

      <Collapsible title="Section Header" defaultOpen>
        <Field label="Eyebrow">
          <input className={inputCls} value={data.eyebrow ?? ''} onChange={e => update({ eyebrow: e.target.value })} />
        </Field>
        <Field label="Title">
          <input className={inputCls} value={data.title ?? ''} onChange={e => update({ title: e.target.value })} />
        </Field>
        <Field label="Title Highlight (gradient)">
          <input className={inputCls} value={data.titleHighlight ?? ''} onChange={e => update({ titleHighlight: e.target.value })} />
        </Field>
        <Field label="Description">
          <textarea className={textareaCls} rows={3} value={data.description ?? ''} onChange={e => update({ description: e.target.value })} />
        </Field>
      </Collapsible>

      <Collapsible title="Industry Cards" badge={`${industries.length}`} defaultOpen>
        {industries.map((ind: any, i: number) => (
          <Collapsible key={i} title={`${ind.emoji || '🏢'} ${ind.name || `Industry ${i + 1}`}`}>
            <div className="flex justify-end mb-1">
              <button type="button" onClick={() => update({ industries: industries.filter((_: any, ii: number) => ii !== i) })}
                className="text-xs text-red-500 hover:text-red-700">✕ Remove</button>
            </div>
            <Field label="Emoji">
              <input className={inputCls} value={ind.emoji ?? ''} onChange={e => updateIndustry(i, 'emoji', e.target.value)} placeholder="⚡" />
            </Field>
            <Field label="Name">
              <input className={inputCls} value={ind.name ?? ''} onChange={e => updateIndustry(i, 'name', e.target.value)} />
            </Field>
            <Field label="Description">
              <textarea className={textareaCls} rows={3} value={ind.description ?? ''} onChange={e => updateIndustry(i, 'description', e.target.value)} />
            </Field>
            <Field label="Link Text">
              <input className={inputCls} value={ind.linkText ?? ''} onChange={e => updateIndustry(i, 'linkText', e.target.value)} />
            </Field>
            <Field label="Link URL">
              <input className={inputCls} value={ind.linkHref ?? ''} onChange={e => updateIndustry(i, 'linkHref', e.target.value)} />
            </Field>
          </Collapsible>
        ))}
        <button type="button"
          onClick={() => update({ industries: [...industries, { emoji: '🏢', name: 'New Industry', description: '', linkText: 'See solutions', linkHref: '#' }] })}
          className="w-full py-2 border border-dashed border-blue-300 dark:border-blue-700 rounded-md text-sm text-blue-600 dark:text-blue-400 hover:bg-blue-50 dark:hover:bg-blue-900/20 transition-colors">
          + Add Industry
        </button>
      </Collapsible>

      <Collapsible title="More Industries Footer" badge={`${moreIndustries.length}`}>
        <Field label="Footer Label">
          <input className={inputCls} value={data.moreLabel ?? ''} onChange={e => update({ moreLabel: e.target.value })} placeholder="Also serving:" />
        </Field>
        {moreIndustries.map((m: any, i: number) => (
          <div key={i} className="flex gap-2 items-center">
            <input className={`flex-1 ${inputCls}`} value={m.name ?? ''} onChange={e => updateMore(i, 'name', e.target.value)} placeholder="Industry name" />
            <input className={`flex-1 ${inputCls}`} value={m.href ?? ''} onChange={e => updateMore(i, 'href', e.target.value)} placeholder="URL" />
            <button type="button" onClick={() => update({ moreIndustries: moreIndustries.filter((_: any, mi: number) => mi !== i) })}
              className="p-1.5 text-red-500 hover:bg-red-50 dark:hover:bg-red-900/20 rounded">✕</button>
          </div>
        ))}
        <button type="button" onClick={() => update({ moreIndustries: [...moreIndustries, { name: '', href: '#' }] })}
          className="w-full py-2 border border-dashed border-gray-300 dark:border-gray-600 rounded-md text-sm text-gray-500 hover:border-blue-400 hover:text-blue-500 transition-colors">
          + Add More Industry
        </button>
      </Collapsible>

      <Collapsible title="Accent Colors">
        <div className="grid grid-cols-2 gap-2">
          {(['accentColor', 'accentColor2'] as const).map((key, i) => (
            <Field key={key} label={['Primary', 'Secondary'][i]}>
              <div className="flex gap-1 items-center">
                <input type="color" value={data[key] ?? '#4F7FFF'} onChange={e => update({ [key]: e.target.value })}
                  className="w-9 h-9 rounded border border-gray-300 dark:border-gray-600 cursor-pointer p-0.5 bg-white dark:bg-gray-700" />
                <input className={inputCls} value={data[key] ?? ''} onChange={e => update({ [key]: e.target.value })} />
              </div>
            </Field>
          ))}
        </div>
      </Collapsible>

    </div>
  );
}
