/**
 * PlatformModulesBentoPropertyPanel
 * Author: generated by Amazon Q
 * Created: 2025-01-01
 * Purpose: Full visual editor for PlatformModulesBentoSection fields
 */

'use client';

import React, { useState } from 'react';
import { SectionConfig } from '@/lib/page-builder/section-schemas';

interface PlatformModulesBentoPropertyPanelProps {
  section: SectionConfig;
  onUpdate: (updates: Partial<SectionConfig>) => void;
}

function Field({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div>
      <label className="block text-xs font-medium text-gray-600 dark:text-gray-400 mb-1">{label}</label>
      {children}
    </div>
  );
}

const inputCls = 'w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-white dark:bg-gray-700 text-gray-900 dark:text-white text-sm';
const textareaCls = inputCls + ' resize-none';

function Collapsible({ title, badge, children, defaultOpen = false }: {
  title: string; badge?: string; children: React.ReactNode; defaultOpen?: boolean;
}) {
  const [open, setOpen] = useState(defaultOpen);
  return (
    <div className="border border-gray-200 dark:border-gray-600 rounded-lg overflow-hidden">
      <button type="button" onClick={() => setOpen(o => !o)}
        className="w-full flex items-center justify-between px-3 py-2.5 bg-gray-50 dark:bg-gray-700 hover:bg-gray-100 dark:hover:bg-gray-600 transition-colors text-left">
        <span className="text-sm font-medium text-gray-800 dark:text-white flex items-center gap-2">
          {title}
          {badge && <span className="text-xs bg-blue-100 dark:bg-blue-900/40 text-blue-700 dark:text-blue-300 px-2 py-0.5 rounded-full">{badge}</span>}
        </span>
        <span className={`text-gray-400 text-xs transition-transform ${open ? 'rotate-90' : ''}`}>▶</span>
      </button>
      {open && <div className="p-3 space-y-3 bg-white dark:bg-gray-800">{children}</div>}
    </div>
  );
}

export function PlatformModulesBentoPropertyPanel({ section, onUpdate }: PlatformModulesBentoPropertyPanelProps) {
  const data = section.data as any;
  const update = (patch: Record<string, any>) => onUpdate({ data: { ...data, ...patch } });
  const modules = data.modules ?? [];

  const updateModule = (idx: number, field: string, val: string) => {
    const next = modules.map((m: any, i: number) => i === idx ? { ...m, [field]: val } : m);
    update({ modules: next });
  };

  const moveModule = (idx: number, dir: -1 | 1) => {
    const next = [...modules];
    const target = idx + dir;
    if (target < 0 || target >= next.length) return;
    [next[idx], next[target]] = [next[target], next[idx]];
    update({ modules: next });
  };

  return (
    <div className="space-y-4">

      <Collapsible title="Section Header" defaultOpen>
        <Field label="Eyebrow">
          <input className={inputCls} value={data.eyebrow ?? ''} onChange={e => update({ eyebrow: e.target.value })} />
        </Field>
        <Field label="Title">
          <input className={inputCls} value={data.title ?? ''} onChange={e => update({ title: e.target.value })} />
        </Field>
        <Field label="Title Highlight (gradient)">
          <input className={inputCls} value={data.titleHighlight ?? ''} onChange={e => update({ titleHighlight: e.target.value })} />
        </Field>
        <Field label="Description">
          <textarea className={textareaCls} rows={2} value={data.description ?? ''} onChange={e => update({ description: e.target.value })} />
        </Field>
      </Collapsible>

      <Collapsible title="Modules" badge={`${modules.length}`} defaultOpen>
        <div className="space-y-3">
          {modules.map((mod: any, idx: number) => (
            <Collapsible key={idx} title={`${mod.size === 'large' ? '🔲' : '▪️'} ${mod.tag || `Module ${idx + 1}`}`}>
              <div className="flex gap-1 mb-2">
                <button type="button" onClick={() => moveModule(idx, -1)} disabled={idx === 0}
                  className="px-2 py-1 text-xs bg-gray-100 dark:bg-gray-700 rounded disabled:opacity-30 hover:bg-gray-200 dark:hover:bg-gray-600">↑ Up</button>
                <button type="button" onClick={() => moveModule(idx, 1)} disabled={idx === modules.length - 1}
                  className="px-2 py-1 text-xs bg-gray-100 dark:bg-gray-700 rounded disabled:opacity-30 hover:bg-gray-200 dark:hover:bg-gray-600">↓ Down</button>
                <button type="button" onClick={() => update({ modules: modules.filter((_: any, i: number) => i !== idx) })}
                  className="ml-auto px-2 py-1 text-xs bg-red-100 dark:bg-red-900/30 text-red-600 dark:text-red-400 rounded hover:bg-red-200">🗑 Remove</button>
              </div>
              <Field label="Size">
                <select className={inputCls} value={mod.size} onChange={e => updateModule(idx, 'size', e.target.value)}>
                  <option value="large">Large (half width)</option>
                  <option value="small">Small (quarter width)</option>
                </select>
              </Field>
              <Field label="Icon (emoji)">
                <input className={inputCls} value={mod.icon ?? ''} onChange={e => updateModule(idx, 'icon', e.target.value)} placeholder="⚡" />
              </Field>
              <Field label="Tag (category label)">
                <input className={inputCls} value={mod.tag ?? ''} onChange={e => updateModule(idx, 'tag', e.target.value)} />
              </Field>
              <Field label="Title">
                <input className={inputCls} value={mod.title ?? ''} onChange={e => updateModule(idx, 'title', e.target.value)} />
              </Field>
              <Field label="Description">
                <textarea className={textareaCls} rows={3} value={mod.description ?? ''} onChange={e => updateModule(idx, 'description', e.target.value)} />
              </Field>
              <Field label="Link Text">
                <input className={inputCls} value={mod.linkText ?? ''} onChange={e => updateModule(idx, 'linkText', e.target.value)} />
              </Field>
              <Field label="Link URL">
                <input className={inputCls} value={mod.linkHref ?? ''} onChange={e => updateModule(idx, 'linkHref', e.target.value)} />
              </Field>
            </Collapsible>
          ))}
        </div>
        <button type="button"
          onClick={() => update({ modules: [...modules, { size: 'small', icon: '⭐', tag: 'New Module', title: 'Module Title', description: 'Module description.', linkText: 'Explore', linkHref: '#' }] })}
          className="w-full py-2.5 border border-dashed border-blue-300 dark:border-blue-700 rounded-md text-sm text-blue-600 dark:text-blue-400 hover:bg-blue-50 dark:hover:bg-blue-900/20 transition-colors font-medium">
          + Add Module
        </button>
      </Collapsible>

      <Collapsible title="Accent Colors">
        <div className="grid grid-cols-2 gap-2">
          {(['accentColor', 'accentColor2'] as const).map((key, i) => (
            <Field key={key} label={['Primary', 'Secondary'][i]}>
              <div className="flex gap-1 items-center">
                <input type="color" value={data[key] ?? '#4F7FFF'} onChange={e => update({ [key]: e.target.value })}
                  className="w-9 h-9 rounded border border-gray-300 dark:border-gray-600 cursor-pointer p-0.5 bg-white dark:bg-gray-700" />
                <input className={inputCls} value={data[key] ?? ''} onChange={e => update({ [key]: e.target.value })} />
              </div>
            </Field>
          ))}
        </div>
      </Collapsible>

    </div>
  );
}
